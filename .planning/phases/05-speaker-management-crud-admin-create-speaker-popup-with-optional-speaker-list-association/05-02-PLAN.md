---
phase: 05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - utils/speakers/manageSpeakerClient.ts
  - utils/speakers/manageSpeakerClient.test.ts
  - components/CreateSpeakerPopup.tsx
  - components/SpeakerTable.tsx
  - pages/admin/speakers.tsx
autonomous: true
requirements: [SPK-02, SPK-03, SPK-05, SPK-06, SPK-07]
must_haves:
  truths:
    - "Admin Speakers page shows a top-level `Add Speaker` button."
    - "Clicking `Add Speaker` opens a popup that captures all speaker info inputs and optional list-association intent."
    - "Submitting popup data passes selected images to backend and can request speaker-list creation."
    - "If a speaker list is created, UI shows the exact required success link and instruction copy."
  artifacts:
    - path: "components/CreateSpeakerPopup.tsx"
      provides: "Create-speaker dialog UI and callable submit flow"
    - path: "components/SpeakerTable.tsx"
      provides: "Top toolbar action for opening create-speaker popup"
    - path: "utils/speakers/manageSpeakerClient.ts"
      provides: "Client callable payload/success-popup contract helpers with exact copy constants"
    - path: "utils/speakers/manageSpeakerClient.test.ts"
      provides: "Regression tests for exact link/text constants and payload shaping"
  key_links:
    - from: "components/SpeakerTable.tsx"
      to: "pages/admin/speakers.tsx"
      via: "toolbar add-button callback -> popup open state"
      pattern: "onAddSpeaker"
    - from: "components/CreateSpeakerPopup.tsx"
      to: "utils/createFunction.ts"
      via: "createFunctionV2('managespeaker') invocation"
      pattern: "createFunctionV2<"
    - from: "components/CreateSpeakerPopup.tsx"
      to: "utils/speakers/manageSpeakerClient.ts"
      via: "success-popup contract constants and response interpretation"
      pattern: "SUBSPLASH_SPEAKER_LIST_LINK|SPEAKER_LIST_SUCCESS_INSTRUCTION"
---

<objective>
Deliver the admin speaker creation UX contract: top add button, popup form, optional speaker-list association, and exact required success popup copy/link behavior.

Purpose: Complete the UI workflow that consumes `managespeaker` so admins can create speakers and optional speaker lists directly from `/admin/speakers`.
Output: New popup component, speakers-page integration, client contract helper/tests, and success popup behavior for list creation.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association/05-RESEARCH.md
@pages/admin/speakers.tsx
@components/SpeakerTable.tsx
@components/PopUp.tsx
@components/ImageViewer.tsx
@types/Speaker.ts
@types/Image.ts
@utils/createFunction.ts

<interfaces>
From `utils/createFunction.ts`:
```typescript
export const createFunctionV2 = <T = any, R = any, M extends object = CallableMutationMetadata>(
  name: string
): ((data: T, options?: CallableCallOptions<M>) => Promise<R>) => { ... };
```

From `types/Image.ts`:
```typescript
export const ImageSizes = ['square', 'wide', 'banner'] as const;
export type ImageSizeType = (typeof ImageSizes)[number];
export type ImageType = { id: string; type: ImageSizeType; ... };
```

From `functions/src/speakers/manageSpeakerTypes.ts` (created in 05-01):
```typescript
export type ManageSpeakerCommand = 'create' | 'update' | 'delete';
export interface ManageSpeakerCreateInput { ... }
export interface ManageSpeakerOutput { speakerListCreated?: boolean; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add client contract helper for create-speaker payload and required success-popup copy</name>
  <files>utils/speakers/manageSpeakerClient.ts, utils/speakers/manageSpeakerClient.test.ts</files>
  <behavior>
    - Helper exports the exact required Subsplash link constant.
    - Helper exports the exact required instruction text constant (verbatim spelling/punctuation).
    - Payload builder preserves selected images and optional create-list toggle for callable input.
  </behavior>
  <action>Create a dedicated client helper module that centralizes `managespeaker` request shaping and response interpretation for UI. Hardcode the exact required success link and instruction string in this helper (single source of truth). Add Jest tests that assert exact string equality and payload mapping to prevent accidental copy drift in future refactors.</action>
  <verify>
    <automated>pnpm exec jest --config jest.config.js utils/speakers/manageSpeakerClient.test.ts --runInBand</automated>
  </verify>
  <done>UI contract helper exists, strings are exact, and tests lock the required copy/link contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Create Speaker popup component with full speaker inputs and optional list association</name>
  <files>components/CreateSpeakerPopup.tsx</files>
  <action>Create a popup component using existing `PopUp` and `ImageViewer` patterns. Include speaker info inputs used for create flow (speaker name, optional sermon-count seed if supported, image selection for square/wide/banner, and toggle/checkbox for optional speaker-list creation). Require square image before enabling submit so selected images can be passed through to backend with clear validation feedback.</action>
  <verify>
    <automated>pnpm exec eslint components/CreateSpeakerPopup.tsx</automated>
  </verify>
  <done>Popup collects complete create-speaker input, supports optional list association intent, and enforces square-image presence for submission.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Add Speaker top-button flow, callable submit, local state refresh, and success popup</name>
  <files>pages/admin/speakers.tsx, components/SpeakerTable.tsx, components/CreateSpeakerPopup.tsx, utils/speakers/manageSpeakerClient.ts</files>
  <action>Add a top-level `Add Speaker` action in the speakers toolbar and wire popup open/close state in `pages/admin/speakers.tsx`. On submit, call `createFunctionV2('managespeaker')` with helper-built payload and selected images passed through unchanged. On success, prepend/refresh speaker table state and total count. If backend response indicates speaker-list creation, show a success popup containing the exact URL `https://dashboard.subsplash.com/-d/#/library/lists/standard/2d040f78-a3e1-447a-b5b3-5e80b608dbc6` and exact required instruction text: `Your speaker list was created sucessfully - please following the subsplash link and add the newly created list to the correct location to the speakers list if you want it to appear there in the app.`</action>
  <verify>
    <automated>pnpm exec eslint pages/admin/speakers.tsx components/SpeakerTable.tsx components/CreateSpeakerPopup.tsx utils/speakers/manageSpeakerClient.ts && pnpm exec jest --config jest.config.js utils/speakers/manageSpeakerClient.test.ts --runInBand</automated>
  </verify>
  <done>The top button launches popup create flow end-to-end, callable submit works, and list-created success popup shows exact link/instruction text.</done>
</task>

</tasks>

<verification>
Run lint and client contract tests, then validate manually in dev that `/admin/speakers` top button -> popup -> create flow works with and without optional list creation.
</verification>

<success_criteria>
- `/admin/speakers` has an obvious top `Add Speaker` action.
- Popup create flow sends selected images plus optional list association intent to `managespeaker`.
- List-created success state always displays the exact required link and instruction copy.
</success_criteria>

<output>
After completion, create `.planning/phases/05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association/05-02-SUMMARY.md`
</output>
