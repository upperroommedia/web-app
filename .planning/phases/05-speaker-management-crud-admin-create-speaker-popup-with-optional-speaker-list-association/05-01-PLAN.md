---
phase: 05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/speakers/createSpeakerTypes.ts
  - functions/src/speakers/speakerMutations.ts
  - functions/src/speakers/createSpeaker.ts
  - functions/src/speakers/updateSpeaker.ts
  - functions/src/speakers/deleteSpeaker.ts
  - functions/src/index.ts
  - functions/src/test/speakers/speakerCrudCallables.test.ts
autonomous: true
requirements: [SPK-01, SPK-04, SPK-05]
must_haves:
  truths:
    - "Admin callable callers can create, update, and delete speakers through separate callable endpoints."
    - "Create flow can optionally create a speaker list and persist the list association on the speaker."
    - "Square image from selected images is reused for both speaker-tag image payload and speaker-list image payload."
  artifacts:
    - path: "functions/src/speakers/createSpeaker.ts"
      provides: "Create-speaker callable with auth checks and create-specific validation"
      exports: ["default"]
    - path: "functions/src/speakers/updateSpeaker.ts"
      provides: "Update-speaker callable with patch validation and immutable-id protections"
      exports: ["default"]
    - path: "functions/src/speakers/deleteSpeaker.ts"
      provides: "Delete-speaker callable with explicit optional associated-list deletion behavior"
      exports: ["default"]
    - path: "functions/src/speakers/speakerMutations.ts"
      provides: "Shared Firestore + optional Subsplash/list mutation orchestration for speaker CRUD"
    - path: "functions/src/test/speakers/speakerCrudCallables.test.ts"
      provides: "Emulator regression coverage for create/update/delete callables, optional list association, and square-image rules"
  key_links:
    - from: "functions/src/speakers/createSpeaker.ts"
      to: "functions/src/speakers/speakerMutations.ts"
      via: "create endpoint delegates to shared mutation helpers"
      pattern: "createSpeakerMutation"
    - from: "functions/src/speakers/updateSpeaker.ts"
      to: "functions/src/speakers/speakerMutations.ts"
      via: "update endpoint delegates to shared mutation helpers"
      pattern: "updateSpeakerMutation"
    - from: "functions/src/speakers/deleteSpeaker.ts"
      to: "functions/src/speakers/speakerMutations.ts"
      via: "delete endpoint delegates to shared mutation helpers"
      pattern: "deleteSpeakerMutation"
    - from: "functions/src/index.ts"
      to: "functions/src/speakers/createSpeaker.ts"
      via: "lower-case callable export for v2 URL routing"
      pattern: "exports\\.createspeaker\\s*=\\s*createspeaker"
    - from: "functions/src/index.ts"
      to: "functions/src/speakers/updateSpeaker.ts"
      via: "lower-case callable export for v2 URL routing"
      pattern: "exports\\.updatespeaker\\s*=\\s*updatespeaker"
    - from: "functions/src/index.ts"
      to: "functions/src/speakers/deleteSpeaker.ts"
      via: "lower-case callable export for v2 URL routing"
      pattern: "exports\\.deletespeaker\\s*=\\s*deletespeaker"
---

<objective>
Implement backend speaker management as separate callable endpoints (`createspeaker`, `updatespeaker`, `deletespeaker`) with optional speaker-list creation/association and image contract enforcement.

Purpose: Move speaker lifecycle mutations to a server-owned boundary so admin UI can use conventionally separated CRUD APIs with deterministic side-effect handling.
Output: New create/update/delete callables, shared mutation helpers, index export wiring, and emulator regression tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association/05-RESEARCH.md
@functions/src/createNewSubsplashList.ts
@functions/src/index.ts
@functions/src/firestoreDataConverter.ts
@types/Speaker.ts
@types/List.ts
@types/Image.ts

<interfaces>
From `types/Speaker.ts`:
```typescript
export interface ISpeaker {
  id: string;
  listId?: string;
  tagId?: string;
  name: string;
  images: ImageType[];
  sermonCount: number;
}
```

From `types/List.ts`:
```typescript
export enum ListType {
  SERIES = 'series',
  SPEAKER_LIST = 'speaker-list',
  TOPIC_LIST = 'topic-list',
  CATEGORY_LIST = 'category-list',
  LATEST = 'latest',
}
```

From `functions/src/createNewSubsplashList.ts`:
```typescript
export interface CreateNewSubsplashListInputType {
  title: string;
  subtitle?: string;
  images?: ImageType[];
  operationKey?: string;
}
export interface CreateNewSubsplashListOutputType {
  listId: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared speaker mutation contracts and validation helpers</name>
  <files>functions/src/speakers/createSpeakerTypes.ts, functions/src/speakers/speakerMutations.ts</files>
  <behavior>
    - Create/update inputs fail with `invalid-argument` when no square image is available in selected images.
    - Optional list-creation and optional associated-list deletion are explicit booleans (no implicit side effects).
    - Shared helper contracts are reusable across all three endpoints.
  </behavior>
  <action>Create typed request/response contracts for `createspeaker`, `updatespeaker`, and `deletespeaker`, including optional `operationKey`, `createSpeakerList`, and list-association/delete fields. Add runtime validation helpers (trimmed-name checks, duplicate-name normalization helper, required square-image resolver) in the shared speaker mutation module so behavior stays consistent across endpoints.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Typed contracts and validation helpers are exported and imported by each callable without `any` casts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `createspeaker`, `updatespeaker`, and `deletespeaker` callable orchestration</name>
  <files>functions/src/speakers/createSpeaker.ts, functions/src/speakers/updateSpeaker.ts, functions/src/speakers/deleteSpeaker.ts, functions/src/speakers/speakerMutations.ts, functions/src/index.ts</files>
  <behavior>
    - `createspeaker` writes speaker doc and optionally creates/associates a speaker list in the same logical flow.
    - `updatespeaker` patches allowed speaker fields and preserves immutable ids.
    - `deletespeaker` removes speaker and supports optional associated-list delete behavior only when explicitly requested.
    - Square image is the image used for both speaker-tag payload and speaker-list payload.
  </behavior>
  <action>Implement separate `onCall` callables that enforce auth/role checks and validate endpoint-specific payloads before invoking shared mutation handlers. For optional speaker-list create in `createspeaker`, call existing `createNewSubsplashList` and persist a Firestore list doc with `ListType.SPEAKER_LIST`, then set `speaker.listId` to that doc id. Reuse existing lock/idempotency utilities when remote side effects are involved. Export endpoints as `exports.createspeaker`, `exports.updatespeaker`, and `exports.deletespeaker` in `functions/src/index.ts` (lower-case route compatibility).</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Create, update, and delete operations execute via separate endpoints, optional list association persists, and square-image routing is enforced server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add emulator regression tests for speaker callables and image/list contracts</name>
  <files>functions/src/test/speakers/speakerCrudCallables.test.ts</files>
  <behavior>
    - Valid create returns success and persists speaker doc with expected fields.
    - Create with `createSpeakerList=true` persists list doc (`speaker-list`) and links `speaker.listId`.
    - Missing square image returns `HttpsError('invalid-argument')` in create/update.
    - Update/delete callables mutate state correctly and reject unauthorized callers.
  </behavior>
  <action>Add focused emulator tests for endpoint auth, validation failures, optional speaker-list association, and state mutation correctness. Stub/mock remote Subsplash calls so tests stay deterministic while still asserting square-image payload propagation and list association behavior.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/speakers/speakerCrudCallables.test.ts --forceExit"</automated>
  </verify>
  <done>New test suite passes and protects create/update/delete callable behavior, optional list-association path, and square-image constraints.</done>
</task>

</tasks>

<verification>
Run the speaker callable emulator suite and confirm `createspeaker`, `updatespeaker`, and `deletespeaker` exports exist in compiled functions index.
</verification>

<success_criteria>
- Backend exposes separate callable endpoints for speaker create/update/delete.
- Optional speaker-list creation persists both list + speaker association and uses square image payloads.
- Emulator tests catch regressions for auth, validation, CRUD behavior, and list/image contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association/05-01-SUMMARY.md`
</output>
