---
phase: 05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/speakers/manageSpeakerTypes.ts
  - functions/src/speakers/manageSpeakerMutations.ts
  - functions/src/speakers/manageSpeaker.ts
  - functions/src/index.ts
  - functions/src/test/speakers/manageSpeaker.test.ts
autonomous: true
requirements: [SPK-01, SPK-04, SPK-05]
must_haves:
  truths:
    - "Admin callable callers can create, update, and delete speakers through one command surface."
    - "Create flow can optionally create a speaker list and persist the list association on the speaker."
    - "Square image from selected images is reused for both speaker-tag image payload and speaker-list image payload."
  artifacts:
    - path: "functions/src/speakers/manageSpeaker.ts"
      provides: "Callable command dispatcher with auth checks and CRUD routing"
      exports: ["default"]
    - path: "functions/src/speakers/manageSpeakerMutations.ts"
      provides: "Firestore + optional Subsplash/list mutation orchestration for speaker CRUD"
    - path: "functions/src/test/speakers/manageSpeaker.test.ts"
      provides: "Emulator regression coverage for CRUD commands, optional list association, and square-image rules"
  key_links:
    - from: "functions/src/speakers/manageSpeaker.ts"
      to: "functions/src/speakers/manageSpeakerMutations.ts"
      via: "command switch delegates to mutation handlers"
      pattern: "switch\\s*\\(data\\.command\\)"
    - from: "functions/src/speakers/manageSpeakerMutations.ts"
      to: "functions/src/createNewSubsplashList.ts"
      via: "optional create-list branch"
      pattern: "createNewSubsplashList"
    - from: "functions/src/index.ts"
      to: "functions/src/speakers/manageSpeaker.ts"
      via: "lower-case callable export for v2 URL routing"
      pattern: "exports\\.managespeaker\\s*=\\s*managespeaker"
---

<objective>
Implement backend speaker management as one callable command surface (`create`, `update`, `delete`) with optional speaker-list creation/association and image contract enforcement.

Purpose: Move speaker lifecycle mutations to a server-owned boundary so admin UI can use one CRUD API with deterministic side-effect handling.
Output: New `managespeaker` callable, mutation helpers, index export wiring, and emulator regression tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association/05-RESEARCH.md
@functions/src/createNewSubsplashList.ts
@functions/src/index.ts
@functions/src/firestoreDataConverter.ts
@types/Speaker.ts
@types/List.ts
@types/Image.ts

<interfaces>
From `types/Speaker.ts`:
```typescript
export interface ISpeaker {
  id: string;
  listId?: string;
  tagId?: string;
  name: string;
  images: ImageType[];
  sermonCount: number;
}
```

From `types/List.ts`:
```typescript
export enum ListType {
  SERIES = 'series',
  SPEAKER_LIST = 'speaker-list',
  TOPIC_LIST = 'topic-list',
  CATEGORY_LIST = 'category-list',
  LATEST = 'latest',
}
```

From `functions/src/createNewSubsplashList.ts`:
```typescript
export interface CreateNewSubsplashListInputType {
  title: string;
  subtitle?: string;
  images?: ImageType[];
  operationKey?: string;
}
export interface CreateNewSubsplashListOutputType {
  listId: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define speaker command contracts and validation/mutation primitives</name>
  <files>functions/src/speakers/manageSpeakerTypes.ts, functions/src/speakers/manageSpeakerMutations.ts</files>
  <behavior>
    - Create command contract supports `create`, `update`, and `delete` discriminated payloads.
    - Create/update paths fail with `invalid-argument` when no square image is available in selected images.
    - Optional list-creation inputs are explicit booleans (no implicit side effects).
  </behavior>
  <action>Create typed request/response contracts for command-style callable payloads, including optional `operationKey`, `createSpeakerList`, and list-association fields. Add runtime validation helpers (trimmed name checks, duplicate-name normalization helper, required square-image resolver) in the same module layer used by mutation handlers. Keep this file focused on pure contracts/helpers so tests can assert behavior without emulator wiring.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Typed contracts and validation helpers are exported and can be imported by callable + tests without `any` casts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `managespeaker` callable and CRUD mutation orchestration</name>
  <files>functions/src/speakers/manageSpeaker.ts, functions/src/speakers/manageSpeakerMutations.ts, functions/src/index.ts</files>
  <behavior>
    - `create` writes speaker doc and optionally creates/associates a speaker list in the same logical flow.
    - `update` patches allowed speaker fields and preserves immutable ids.
    - `delete` removes speaker and supports optional associated-list delete behavior only when explicitly requested.
    - Square image is the image used for both speaker-tag payload and speaker-list payload.
  </behavior>
  <action>Implement an `onCall` callable that enforces auth/role checks, validates command payloads, and routes to create/update/delete mutation handlers. For optional speaker-list create, call existing `createNewSubsplashList` and persist a Firestore list doc with `ListType.SPEAKER_LIST`, then set `speaker.listId` to that doc id. Reuse existing lock/idempotency utilities when remote side effects are involved, and return a structured response containing whether list creation happened plus the created ids needed by UI. Export callable as `exports.managespeaker` in `functions/src/index.ts` (lower-case route compatibility).</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>`create`, `update`, and `delete` commands execute from one callable endpoint, optional list association persists, and square-image routing is enforced server-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add emulator regression tests for speaker callable CRUD and image/list contracts</name>
  <files>functions/src/test/speakers/manageSpeaker.test.ts</files>
  <behavior>
    - Valid create returns success and persists speaker doc with expected fields.
    - Create with `createSpeakerList=true` persists list doc (`speaker-list`) and links `speaker.listId`.
    - Missing square image returns `HttpsError('invalid-argument')`.
    - Update/delete commands mutate state correctly and reject unauthorized callers.
  </behavior>
  <action>Add focused emulator tests for callable command routing, validation failures, optional speaker-list association, and auth constraints. Stub/mock remote Subsplash calls so tests stay deterministic while still asserting square-image payload propagation and list association behavior. Keep tests under `functions/src/test/speakers` to match existing test organization.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/speakers/manageSpeaker.test.ts --forceExit"</automated>
  </verify>
  <done>New test suite passes and protects CRUD command behavior, optional list-association path, and square-image constraints.</done>
</task>

</tasks>

<verification>
Run the speaker callable emulator suite and confirm `managespeaker` export exists in compiled functions index.
</verification>

<success_criteria>
- Backend exposes a single callable command surface for speaker create/update/delete.
- Optional speaker-list creation persists both list + speaker association and uses square image payloads.
- Emulator tests catch regressions for auth, validation, CRUD behavior, and list/image contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-speaker-management-crud-admin-create-speaker-popup-with-optional-speaker-list-association/05-01-SUMMARY.md`
</output>
