---
phase: 04-role-based-invite-onboarding-and-operational-notification-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - functions/src/invites/inviteTypes.ts
  - functions/src/invites/inviteToken.ts
  - functions/src/invites/createInvite.ts
  - functions/src/invites/claimInvite.ts
  - functions/src/test/invites/createInvite.test.ts
  - functions/src/test/invites/claimInvite.test.ts
autonomous: true
requirements:
  - INVITE-01
  - INVITE-02
  - INVITE-03
must_haves:
  truths:
    - "Admins can issue role-targeted invite links for supported roles with 30-day expiry."
    - "Invite claims require authenticated strict email match and are single-use."
    - "Claim success assigns role immediately, upgrades existing user claims in place, and never downgrades a higher role."
  artifacts:
    - path: "functions/src/invites/inviteTypes.ts"
      provides: "Invite schema, role precedence map, and claim status contracts"
      contains: "ROLE_PRECEDENCE"
    - path: "functions/src/invites/inviteToken.ts"
      provides: "Secure raw token generation and sha256 hash helpers"
      contains: "createHash('sha256')"
    - path: "functions/src/invites/createInvite.ts"
      provides: "Admin callable issuing single-use 30-day invite artifacts"
      exports: ["default"]
    - path: "functions/src/invites/claimInvite.ts"
      provides: "Transactional claim/consume flow with role claim update and refresh-token revocation"
      exports: ["default"]
  key_links:
    - from: "functions/src/invites/createInvite.ts"
      to: "functions/src/invites/inviteToken.ts"
      via: "raw token hash persistence"
      pattern: "hashInviteToken\\("
    - from: "functions/src/invites/claimInvite.ts"
      to: "firebaseAdmin.auth\\(\\)"
      via: "setCustomUserClaims + revokeRefreshTokens"
      pattern: "setCustomUserClaims|revokeRefreshTokens"
    - from: "functions/src/invites/claimInvite.ts"
      to: "Firestore transaction"
      via: "single-use state transition"
      pattern: "runTransaction"
---

<objective>
Implement secure invite issuance and claim backend flows that enforce email match, expiry, and no-downgrade role assignment.

Purpose: Allow admins to onboard users directly into target roles without a separate approval queue.
Output: Invite domain contracts, token security helpers, invite callables, and emulator tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-CONTEXT.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-RESEARCH.md
@functions/src/setUserRole.ts
@functions/src/setUserRoleOnCreate.ts
@types/User.ts
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-01-PLAN.md

<interfaces>
From `types/User.ts`:
```typescript
export const UserRole = {
  ADMIN: 'admin',
  UPLOADER: 'uploader',
  USER: 'user',
  PUBLISHER: 'publisher',
} as const;
export type UserRoleType = (typeof UserRole)[keyof typeof UserRole];
```

From `functions/src/setUserRole.ts`:
```typescript
await auth.setCustomUserClaims(user.uid, { role: request.data.role });
await auth.revokeRefreshTokens(user.uid);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define invite contracts, role precedence, and token helpers</name>
  <files>functions/src/invites/inviteTypes.ts, functions/src/invites/inviteToken.ts</files>
  <action>Create invite type contracts including status fields (`PENDING`, `ROLE_PENDING`, `COMPLETE`, `ROLE_FAILED`), required metadata, and role precedence (`user &lt; uploader &lt; publisher &lt; admin`) used to enforce no-downgrade behavior. Add helper functions for secure token generation (`randomBytes`) and sha256 hashing; store hashes only, never raw token values.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Invite contracts and token helpers compile and expose reusable primitives for issue/claim flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement createInvite and claimInvite callables with transactional consume semantics</name>
  <files>functions/src/invites/createInvite.ts, functions/src/invites/claimInvite.ts</files>
  <behavior>
    - Test 1: Admin createInvite stores hashed token artifact with 30-day expiry and returns raw claim URL.
    - Test 2: claimInvite rejects unauthenticated users, email mismatches, expired invites, and already-consumed invites.
    - Test 3: claimInvite consumes invite once, preserves highest role (no downgrade), and revokes refresh tokens after claim updates.
  </behavior>
  <action>Implement `createInvite` as admin-only callable that validates target email/role, writes invite artifact in Firestore, and returns claim URL containing raw token. Implement `claimInvite` as authenticated callable that hashes provided token, transactionally validates invite state/email/expiry/single-use constraints, marks consume state, resolves final role by precedence, merges claims, applies `setCustomUserClaims`, and revokes refresh tokens. On assignment failure after consume, mark `ROLE_FAILED` for safe retry by same claimant.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Invite callables enforce all locked invite/claim decisions with transactional correctness and no plaintext token storage.</done>
</task>

<task type="auto">
  <name>Task 3: Add invite emulator tests for issue/claim correctness</name>
  <files>functions/src/test/invites/createInvite.test.ts, functions/src/test/invites/claimInvite.test.ts</files>
  <action>Add emulator tests for admin-only invite issuance, token hashing, expiry enforcement, strict email match, single-use transition, existing-user upgrade behavior, and no-downgrade role precedence. Include race-focused assertions proving repeated claim attempts fail after first success.</action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,auth --config ../firebase.test.json "pnpm exec jest --forceExit src/test/invites/createInvite.test.ts src/test/invites/claimInvite.test.ts"</automated>
  </verify>
  <done>Invite test suites pass and lock critical lifecycle invariants for create/claim behavior.</done>
</task>

</tasks>

<verification>
Invite backend callables are test-covered and enforce secure single-use claim semantics with immediate role assignment.
</verification>

<success_criteria>
Admins can issue secure role invites, and authenticated invitees can claim once with email match and no downgrade risk.
</success_criteria>

<output>
After completion, create `.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-03-SUMMARY.md`
</output>
