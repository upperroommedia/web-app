---
phase: 04-role-based-invite-onboarding-and-operational-notification-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/notifications/notificationTypes.ts
  - functions/src/notifications/notificationParams.ts
  - functions/src/notifications/queueEmail.ts
  - functions/src/notifications/emitOperationalAlert.ts
  - functions/src/test/notifications/queueEmail.test.ts
  - functions/src/test/notifications/emitOperationalAlert.test.ts
autonomous: true
requirements:
  - ROLE-REQ-02
  - OPS-ALERT-02
user_setup:
  - service: firebase/firestore-send-email
    why: "Deliver queued outbound emails from Firestore outbox documents."
    dashboard_config:
      - task: "Install/configure the Trigger Email extension for the target Firebase project."
        location: "Firebase Console -> Extensions -> Trigger Email (firestore-send-email)"
must_haves:
  truths:
    - "Notification recipients are environment-configurable for role requests and operational alerts."
    - "Every alert emission writes structured log context and queues an email message."
    - "Alerting sends one message per caught occurrence (no dedupe suppression)."
  artifacts:
    - path: "functions/src/notifications/notificationParams.ts"
      provides: "Typed params/default recipient lists for role request and runtime alerts"
      contains: "defineList"
    - path: "functions/src/notifications/queueEmail.ts"
      provides: "Firestore outbox writer for the Trigger Email extension"
      contains: "collection('mail')"
    - path: "functions/src/notifications/emitOperationalAlert.ts"
      provides: "Shared runtime error alert helper with structured logger payload"
      contains: "logger.error"
    - path: "functions/src/test/notifications/emitOperationalAlert.test.ts"
      provides: "Regression tests for non-deduped alert enqueue behavior"
      contains: "toHaveBeenCalledTimes"
  key_links:
    - from: "functions/src/notifications/emitOperationalAlert.ts"
      to: "functions/src/notifications/queueEmail.ts"
      via: "helper call"
      pattern: "await queueEmail\\("
    - from: "functions/src/notifications/notificationParams.ts"
      to: "functions/src/notifications/emitOperationalAlert.ts"
      via: "recipient parameter resolution"
      pattern: "runtimeAlertRecipients\\.value\\(\\)"
---

<objective>
Establish the notification foundation shared by role-request emails and runtime operational alerts.

Purpose: Create one reusable, environment-configurable routing path for outbound operational messaging.
Output: Parameterized recipient contracts, Firestore email outbox writer, operational alert helper, and focused tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-CONTEXT.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-RESEARCH.md
@functions/src/handleError.ts
@functions/package.json

<interfaces>
From `types/Function.ts`:
```typescript
export type FunctionOutputType<T> =
  | { status: 'success'; data: T }
  | { status: 'error'; error: string };
```

From `functions/src/handleError.ts`:
```typescript
const handleError = (error: unknown): HttpsError => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define notification contracts and runtime params</name>
  <files>functions/src/notifications/notificationTypes.ts, functions/src/notifications/notificationParams.ts</files>
  <action>Create typed notification payload contracts for role-request notifications and operational runtime alerts. Define recipient params via `firebase-functions/params` (`defineList`) with defaults that include `youssef.a.asaad@gmail.com` and `contact@upperroommedia.org` for role-request production routing. Add explicit params for admin base URL and alert recipients, and document that dedupe is intentionally not performed.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Notification contracts compile, params resolve through typed `value()` accessors, and required production-default role-request recipients are encoded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Firestore outbox queue and operational alert helper</name>
  <files>functions/src/notifications/queueEmail.ts, functions/src/notifications/emitOperationalAlert.ts</files>
  <behavior>
    - Test 1: Queue helper writes a mail document containing recipient list and message payload.
    - Test 2: Alert helper logs structured error metadata and enqueues an email for each invocation.
    - Test 3: Repeated identical error invocations enqueue repeated alert messages (no dedupe window).
  </behavior>
  <action>Implement `queueEmail()` to write extension-compatible documents to the `mail` collection with metadata fields for source and alert type. Implement `emitOperationalAlert()` to standardize alert code/context payloads, write structured `logger.error` entries, and call `queueEmail()` every time it is invoked. Keep helper pure/reusable so existing and new callables can import without local transport logic.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Both helpers compile and provide a single reusable API for runtime alerting and email queueing with one-alert-per-occurrence semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add notification utility emulator tests</name>
  <files>functions/src/test/notifications/queueEmail.test.ts, functions/src/test/notifications/emitOperationalAlert.test.ts</files>
  <action>Add emulator-backed tests that verify outbox document shape, required payload fields, and repeated-call behavior for alerts. Include failure-path expectations so alert helper surfaces queue errors without silently swallowing them.</action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore --config ../firebase.test.json "pnpm exec jest --forceExit src/test/notifications/queueEmail.test.ts src/test/notifications/emitOperationalAlert.test.ts"</automated>
  </verify>
  <done>New notification test files pass and lock in queue payload contracts and per-occurrence alert emission behavior.</done>
</task>

</tasks>

<verification>
`notificationParams` returns configured recipients and admin URL, and notification utilities pass targeted emulator tests.
</verification>

<success_criteria>
Shared notification infrastructure exists, is test-covered, and is ready for role-request and runtime-error integrations without introducing direct SMTP logic in callables.
</success_criteria>

<output>
After completion, create `.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-01-SUMMARY.md`
</output>
