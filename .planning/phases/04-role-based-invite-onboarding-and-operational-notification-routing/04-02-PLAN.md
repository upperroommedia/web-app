---
phase: 04-role-based-invite-onboarding-and-operational-notification-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - functions/src/roleRequests/roleRequestTypes.ts
  - functions/src/roleRequests/createRoleRequest.ts
  - functions/src/test/roleRequests/createRoleRequest.test.ts
autonomous: true
requirements:
  - ROLE-REQ-01
  - ROLE-REQ-02
must_haves:
  truths:
    - "Authenticated users can create a new role request with requested role and reason."
    - "New role requests trigger notification enqueue with requester identity, requested role, timestamp, and admin link."
    - "If notification enqueue fails, the role request remains persisted and an operational alert is emitted."
  artifacts:
    - path: "functions/src/roleRequests/roleRequestTypes.ts"
      provides: "Role request request/response and persisted document contracts"
      contains: "CreateRoleRequestInputType"
    - path: "functions/src/roleRequests/createRoleRequest.ts"
      provides: "Callable role-request creation flow with non-blocking notification side effect"
      exports: ["default"]
    - path: "functions/src/test/roleRequests/createRoleRequest.test.ts"
      provides: "Regression tests for persistence-first behavior and enqueue-failure fallback"
      contains: "ROLE_REQUEST_EMAIL_ENQUEUE_FAILED"
  key_links:
    - from: "functions/src/roleRequests/createRoleRequest.ts"
      to: "functions/src/notifications/queueEmail.ts"
      via: "post-write notification enqueue"
      pattern: "await queueEmail\\("
    - from: "functions/src/roleRequests/createRoleRequest.ts"
      to: "functions/src/notifications/emitOperationalAlert.ts"
      via: "enqueue failure fallback"
      pattern: "emitOperationalAlert\\('ROLE_REQUEST_EMAIL_ENQUEUE_FAILED'"
---

<objective>
Implement backend role-request creation with guaranteed persistence and non-blocking notification fanout.

Purpose: Replace the current placeholder request flow with a production-safe role-request pipeline.
Output: Typed role-request callable and tests proving records persist even when notification enqueue fails.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-CONTEXT.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-RESEARCH.md
@functions/src/listUsers.ts
@functions/src/getUser.ts
@functions/src/handleError.ts
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-01-PLAN.md

<interfaces>
From `types/Function.ts`:
```typescript
export type FunctionOutputType<T> =
  | { status: 'success'; data: T }
  | { status: 'error'; error: string };
```

From `context/types.ts`:
```typescript
export const ROLES = ['user', 'admin', 'uploader', 'publisher'];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define role-request contracts and stored document schema</name>
  <files>functions/src/roleRequests/roleRequestTypes.ts</files>
  <action>Create explicit request/response/persistence types for role requests, including `requestedRole`, `reason`, requester identity fields, timestamps, status, and `notification` metadata. Exclude admin role from requestable targets and encode validation helpers shared by callable/tests.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Role-request contract file exports typed interfaces and validators used by callable and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement persistence-first role request callable with notification fallback</name>
  <files>functions/src/roleRequests/createRoleRequest.ts</files>
  <behavior>
    - Test 1: Valid authenticated request persists a roleRequest record and returns success.
    - Test 2: Notification enqueue is attempted only on new request creation and payload includes requester identity, role, timestamp, and admin link.
    - Test 3: Queue failure still returns success for persisted request while emitting operational alert.
  </behavior>
  <action>Implement `createRoleRequest` callable using v2 onCall patterns. Require auth, strict role validation (no `admin`), and trim/sanitize reason text. Persist role request first in Firestore, then attempt `queueEmail` using recipients from params and required payload details. If queueing fails, do not roll back request; call `emitOperationalAlert('ROLE_REQUEST_EMAIL_ENQUEUE_FAILED', ...)` and return a success payload with warning metadata.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Callable persists requests independently from notification transport success and emits alert signals for enqueue failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add emulator tests for createRoleRequest behavior</name>
  <files>functions/src/test/roleRequests/createRoleRequest.test.ts</files>
  <action>Add focused callable tests validating auth guardrails, role validation, required payload fields, first-write persistence, notification payload content, and enqueue-failure fallback. Mock queue helper failures and assert role request records are still present with expected status metadata.</action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,auth --config ../firebase.test.json "pnpm exec jest --forceExit src/test/roleRequests/createRoleRequest.test.ts"</automated>
  </verify>
  <done>Role-request test suite passes and proves non-blocking notification behavior.</done>
</task>

</tasks>

<verification>
Role-request creation is test-covered, writes expected records, and preserves core flow when notification transport fails.
</verification>

<success_criteria>
Backend role-request flow is production-ready and aligned with locked notification-routing and failure-handling decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-02-SUMMARY.md`
</output>
