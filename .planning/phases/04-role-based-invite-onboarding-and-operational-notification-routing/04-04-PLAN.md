---
phase: 04-role-based-invite-onboarding-and-operational-notification-routing
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - functions/src/index.ts
  - components/RequestUploadPrivalige.tsx
  - pages/admin/users.tsx
  - components/UserTable.tsx
  - components/Login.tsx
  - pages/invite/claim.tsx
  - pages/invite/success.tsx
autonomous: true
requirements:
  - INVITE-03
  - ROLE-REQ-01
must_haves:
  truths:
    - "Admins can generate invite links from the existing admin users surface."
    - "Users can submit role requests from the existing request form instead of seeing a disabled placeholder."
    - "Invite claim flow redirects through auth and lands on a dedicated invite success page after claim completion."
  artifacts:
    - path: "functions/src/index.ts"
      provides: "Exported callables for createRoleRequest, createInvite, and claimInvite"
      contains: "exports.createinvite|exports.claiminvite|exports.createrolerequest"
    - path: "components/RequestUploadPrivalige.tsx"
      provides: "Functional role-request submit UI"
      contains: "createFunctionV2('createrolerequest')"
    - path: "pages/admin/users.tsx"
      provides: "Admin invite issuance UX with generated invite link handling"
      contains: "createFunctionV2('createinvite')"
    - path: "pages/invite/claim.tsx"
      provides: "Token claim route that enforces auth and calls claim callable"
      contains: "createFunctionV2('claiminvite')"
    - path: "pages/invite/success.tsx"
      provides: "Post-claim destination page"
      contains: "Invite"
  key_links:
    - from: "pages/invite/claim.tsx"
      to: "components/Login.tsx"
      via: "callback routing with invite token parameters"
      pattern: "callbackurl|callbackUrl"
    - from: "pages/admin/users.tsx"
      to: "functions/src/index.ts"
      via: "createinvite callable endpoint"
      pattern: "createFunctionV2\\('createinvite'\\)"
    - from: "components/RequestUploadPrivalige.tsx"
      to: "functions/src/index.ts"
      via: "createrolerequest callable endpoint"
      pattern: "createFunctionV2\\('createrolerequest'\\)"
---

<objective>
Wire invite and role-request backend surfaces into callable exports and UI routes.

Purpose: Make phase capabilities reachable by admins and end users in existing product flows.
Output: Callable exports, admin invite UX, functional role-request form, invite claim route, and success page.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-CONTEXT.md
@functions/src/index.ts
@pages/admin/users.tsx
@components/UserTable.tsx
@components/RequestUploadPrivalige.tsx
@components/Login.tsx
@utils/createFunction.ts
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-02-PLAN.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-03-PLAN.md

<interfaces>
From `utils/createFunction.ts`:
```typescript
export const createFunctionV2 = <T = any, R = any>(name: string): ((data: T) => Promise<R>) => { ... };
```

From `components/Login.tsx`:
```typescript
const { callbackurl: possibleCallback } = router.query;
```

From `functions/src/index.ts` export style:
```typescript
exports.setuserrole = setuserrole;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export new role-request and invite callables</name>
  <files>functions/src/index.ts</files>
  <action>Import and export `createrolerequest`, `createinvite`, and `claiminvite` using the existing lower-case export naming convention in `functions/src/index.ts`. Keep naming consistent with `createFunctionV2` call sites and avoid introducing alternate alias formats that break existing URL routing assumptions.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>New callable exports are compiled and reachable via v2 function URL naming conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin invite issuance UI and functional role-request submit flow</name>
  <files>pages/admin/users.tsx, components/UserTable.tsx, components/RequestUploadPrivalige.tsx</files>
  <behavior>
    - Test 1: Admin can submit invite email + role and receives generated invite URL from callable response.
    - Test 2: Non-upload user can submit role request and sees success/error feedback from callable result.
    - Test 3: Role-request form no longer displays disabled placeholder-only behavior.
  </behavior>
  <action>Replace placeholder behavior in `RequestUploadPrivalige` with a real submit handler calling `createFunctionV2('createrolerequest')`, including loading and response feedback. In admin users UI, add an invite issuance action (button/dialog) that captures invitee email + target role, calls `createFunctionV2('createinvite')`, and surfaces the generated claim link for copy/use. Keep role dropdown behavior intact and avoid changing unrelated user table sorting/pagination logic.</action>
  <verify>
    <automated>pnpm exec next lint --file components/RequestUploadPrivalige.tsx --file pages/admin/users.tsx --file components/UserTable.tsx</automated>
  </verify>
  <done>Admin invite and user role-request interactions are wired to real callables with visible success/failure feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement invite claim and success routes with login callback continuity</name>
  <files>pages/invite/claim.tsx, pages/invite/success.tsx, components/Login.tsx</files>
  <behavior>
    - Test 1: Unauthenticated invite claim route redirects to login while preserving invite token callback state.
    - Test 2: After auth, claim route calls `claiminvite`, refreshes ID token, and routes to invite success page.
    - Test 3: Claim failures (expired, mismatch, consumed) render actionable error state without silent redirects.
  </behavior>
  <action>Create `/invite/claim` route that reads token params, ensures auth, executes `createFunctionV2('claiminvite')`, and handles loading/success/error states. Create `/invite/success` page as the dedicated post-claim destination. Update login callback parsing to preserve invite callback paths robustly (support existing `callbackurl` behavior while tolerating `callbackUrl` aliases) so invite claims resume correctly after sign-in.</action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Invite links can be claimed end-to-end through auth redirect and users land on a dedicated success page.</done>
</task>

</tasks>

<verification>
Callable exports compile, admin can issue invite links, and invite claim/login callback flow resolves to success page.
</verification>

<success_criteria>
Phase 4 backend features are accessible through the existing UI and a dedicated invite onboarding route.
</success_criteria>

<output>
After completion, create `.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-04-SUMMARY.md`
</output>
