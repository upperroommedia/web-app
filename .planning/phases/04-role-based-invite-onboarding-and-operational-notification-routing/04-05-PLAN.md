---
phase: 04-role-based-invite-onboarding-and-operational-notification-routing
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - functions/src/uploadToSubsplash.ts
  - functions/src/editSubsplashSermon.ts
  - functions/src/deleteFromSubsplash.ts
  - functions/src/uploadToSoundCloud.ts
  - functions/src/editSoundCloudSermon.ts
  - functions/src/deleteFromSoundCloud.ts
  - functions/src/addIntroOutro/addintrooutrotaskgenerator.ts
  - functions/src/addIntroOutro/addintrooutrotaskhandler.ts
  - functions/src/test/notifications/runtimeAlerts.test.ts
autonomous: true
requirements:
  - OPS-ALERT-01
  - OPS-ALERT-02
must_haves:
  truths:
    - "Caught runtime errors in upload/audio processing and external publish callables emit operational alerts."
    - "Each caught occurrence emits a distinct alert event (no dedupe)."
    - "Alert payloads include enough function/entity context for operator triage."
  artifacts:
    - path: "functions/src/test/notifications/runtimeAlerts.test.ts"
      provides: "Regression coverage for alert helper invocation from catch paths"
      contains: "emitOperationalAlert"
    - path: "functions/src/addIntroOutro/addintrooutrotaskhandler.ts"
      provides: "Operational alert emission from task processing catch path"
      contains: "emitOperationalAlert"
    - path: "functions/src/uploadToSubsplash.ts"
      provides: "Operational alert emission for caught upload failures"
      contains: "emitOperationalAlert"
  key_links:
    - from: "functions/src/uploadToSubsplash.ts"
      to: "functions/src/notifications/emitOperationalAlert.ts"
      via: "catch path alert call"
      pattern: "catch \\(error\\).*emitOperationalAlert"
    - from: "functions/src/addIntroOutro/addintrooutrotaskhandler.ts"
      to: "functions/src/notifications/emitOperationalAlert.ts"
      via: "task failure alerting"
      pattern: "emitOperationalAlert\\('AUDIO_"
---

<objective>
Roll out centralized operational alerting across existing caught runtime error paths in critical publish and audio-processing flows.

Purpose: Ensure operators are notified for every runtime failure, not only a small critical subset.
Output: Alert-helper integration in catch paths and regression tests proving alert invocation behavior.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-CONTEXT.md
@functions/src/uploadToSubsplash.ts
@functions/src/editSubsplashSermon.ts
@functions/src/deleteFromSubsplash.ts
@functions/src/uploadToSoundCloud.ts
@functions/src/editSoundCloudSermon.ts
@functions/src/deleteFromSoundCloud.ts
@functions/src/addIntroOutro/addintrooutrotaskgenerator.ts
@functions/src/addIntroOutro/addintrooutrotaskhandler.ts
@.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-01-PLAN.md

<interfaces>
From `functions/src/handleError.ts`:
```typescript
const handleError = (error: unknown): HttpsError => { ... };
```

From `functions/src/notifications/emitOperationalAlert.ts` (from plan 04-01):
```typescript
emitOperationalAlert(code: string, error: unknown, context: Record<string, unknown>): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define runtime alert taxonomy for existing catch paths</name>
  <files>functions/src/test/notifications/runtimeAlerts.test.ts</files>
  <action>Create a focused test scaffold that enumerates expected alert codes/context fields for publish and audio-processing paths. Use this as the contract for rollout coverage so each targeted catch block emits a deterministic alert code and includes function/entity identifiers.</action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Runtime alert contract tests compile and declare expected per-function alert emissions before implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate operational alerts into external publish catch paths</name>
  <files>functions/src/uploadToSubsplash.ts, functions/src/editSubsplashSermon.ts, functions/src/deleteFromSubsplash.ts, functions/src/uploadToSoundCloud.ts, functions/src/editSoundCloudSermon.ts, functions/src/deleteFromSoundCloud.ts</files>
  <behavior>
    - Test 1: Each callable catch path emits `emitOperationalAlert` with function-specific code and request context.
    - Test 2: Alert emission occurs for every caught error occurrence without dedupe suppression.
    - Test 3: Existing error responses remain unchanged (`handleError`/HttpsError behavior preserved).
  </behavior>
  <action>Import `emitOperationalAlert` into each targeted publish callable and invoke it in existing catch blocks before rethrowing normalized errors. Include contextual metadata such as callable name, operation key (when present), and entity IDs to support triage. Do not alter locked lock/idempotency payload contracts or existing response/error code behavior.</action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore --config ../firebase.test.json "pnpm exec jest --forceExit src/test/notifications/runtimeAlerts.test.ts"</automated>
  </verify>
  <done>All targeted publish callables emit operational alerts on catch while preserving existing response semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate operational alerts into add-intro/outro generator and handler failure paths</name>
  <files>functions/src/addIntroOutro/addintrooutrotaskgenerator.ts, functions/src/addIntroOutro/addintrooutrotaskhandler.ts, functions/src/test/notifications/runtimeAlerts.test.ts</files>
  <behavior>
    - Test 1: Generator catch path emits alert with task input context when validation/queueing fails.
    - Test 2: Handler catch path emits alert with sermon/task identifiers before final status update attempts.
    - Test 3: Repeated task failures produce repeated alert emissions.
  </behavior>
  <action>Add alert emissions in add-intro/outro generator/handler catch paths using explicit alert codes and context fields (`sermonId`, `audio source`, task route). Keep existing status update logic intact so current user-facing error states continue to work while adding operational visibility for each caught failure occurrence.</action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore --config ../firebase.test.json "pnpm exec jest --forceExit src/test/notifications/runtimeAlerts.test.ts"</automated>
  </verify>
  <done>Audio-processing failure paths emit operational alerts per occurrence without regressing existing status/error handling behavior.</done>
</task>

</tasks>

<verification>
All targeted runtime catch paths emit structured alert events and notification queue writes for every occurrence.
</verification>

<success_criteria>
Operational failure visibility is consistently routed through centralized alert utilities across critical publishing and audio-processing code paths.
</success_criteria>

<output>
After completion, create `.planning/phases/04-role-based-invite-onboarding-and-operational-notification-routing/04-05-SUMMARY.md`
</output>
