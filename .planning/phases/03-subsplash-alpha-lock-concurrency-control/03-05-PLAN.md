---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - utils/createFunction.ts
  - utils/callableConcurrency.ts
  - utils/callableConcurrency.test.ts
  - components/ManagePublishingPopup.tsx
  - pages/admin/series/[seriesId].tsx
  - pages/admin/sermons/[sermonId].tsx
  - pages/api/uploadFile.tsx
  - pages/api/editSermon.ts
autonomous: true
requirements:
  - LOCK-03
  - LOCK-04
  - LOCK-05
user_setup: []
must_haves:
  truths:
    - "UI/API callers send operation keys on mutation calls so retries are idempotent."
    - "Lock-busy payloads are surfaced as actionable retry guidance rather than generic failures."
    - "Retry behavior stays caller-controlled in admin workflows."
    - "Client-side integration for lock contention is covered by automated tests."
  artifacts:
    - path: "utils/callableConcurrency.ts"
      provides: "Shared operation-key generation and busy-error parsing helpers."
      exports: ["createOperationKey", "parseLockBusyDetails"]
    - path: "utils/createFunction.ts"
      provides: "Callable wrapper extension to support mutation metadata headers/payload."
    - path: "components/ManagePublishingPopup.tsx"
      provides: "Admin mutation flows wired with operation keys and busy-state handling."
    - path: "pages/admin/series/[seriesId].tsx"
      provides: "Series admin mutation retry handling using lock-busy details."
    - path: "pages/admin/sermons/[sermonId].tsx"
      provides: "Sermon admin mutation retry handling using lock-busy details."
    - path: "utils/callableConcurrency.test.ts"
      provides: "Deterministic tests for busy-detail parsing and key generation."
  key_links:
    - from: "components/ManagePublishingPopup.tsx"
      to: "utils/callableConcurrency.ts"
      via: "operation key generation per mutation intent"
      pattern: "createOperationKey"
    - from: "pages/admin/series/[seriesId].tsx"
      to: "callable HttpsError.details"
      via: "busy payload parsing and retry message"
      pattern: "parseLockBusyDetails"
    - from: "pages/api/editSermon.ts"
      to: "editSubsplashSermon callable"
      via: "operation key propagation"
      pattern: "operationKey"
---

<objective>
Wire caller-side mutation flows to the new lock/idempotency contract so backend guarantees are actually used in production paths.

Purpose: complete end-to-end contract adoption by adding operation-key propagation and actionable lock-contention handling in admin workflows.
Output: shared client helpers, updated mutation callers, and automated contract tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@components/ManagePublishingPopup.tsx
@pages/admin/series/[seriesId].tsx
@pages/admin/sermons/[sermonId].tsx
@pages/api/uploadFile.tsx
@pages/api/editSermon.ts
@utils/createFunction.ts

<interfaces>
From utils/createFunction.ts:
```typescript
export const createFunctionV2 = <T = any, R = any>(name: string): ((data: T) => Promise<R>) => {
  // httpsCallableFromURL wrapper
};
```

From pages/api/editSermon.ts:
```typescript
const editSubsplashSermon = createFunctionV2<EDIT_SUBSPLASH_SERMON_INCOMING_DATA>('editSubsplashSermon');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared caller utilities for operation keys and busy detail parsing</name>
  <files>utils/callableConcurrency.ts, utils/callableConcurrency.test.ts, utils/createFunction.ts</files>
  <behavior>
    - Test 1: `createOperationKey` returns stable-format unique keys.
    - Test 2: Busy-detail parser extracts `locked_keys`, `wait_ms`, and `retry_after_ms` from callable errors.
    - Test 3: Non-busy errors are not misclassified as lock contention.
  </behavior>
  <action>
    Create a shared helper module used by all admin mutation call sites:
    - `createOperationKey(scope, entityId)` for per-operation idempotency keys.
    - `parseLockBusyDetails(error)` that decodes callable `HttpsError.details`.

    Extend `createFunctionV2` typing and optional metadata plumbing so call sites can consistently pass operation key fields without ad-hoc casting.
  </action>
  <verify>
    <automated>pnpm exec jest --runInBand --forceExit utils/callableConcurrency.test.ts</automated>
  </verify>
  <done>
    Shared caller utilities exist, are tested, and provide one canonical way to generate operation keys and interpret busy contention details.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate operation keys and busy-retry UX across admin mutation surfaces</name>
  <files>components/ManagePublishingPopup.tsx, pages/admin/series/[seriesId].tsx, pages/admin/sermons/[sermonId].tsx, pages/api/uploadFile.tsx, pages/api/editSermon.ts</files>
  <behavior>
    - Test 1: Each migrated mutation call includes `operationKey` in request payload.
    - Test 2: Busy contention shows retry guidance using `retry_after_ms` (no hidden auto-retry loop).
    - Test 3: Existing success paths remain unchanged when no contention occurs.
  </behavior>
  <action>
    Update all listed caller surfaces to:
    - Generate operation keys per mutation intent.
    - Pass operation keys into migrated callables (`addtolist`, `removefromlist`, `uploadToSubsplash`, `addtoseries`, `removefromseries`, `reorderseriesitems`, `editSubsplashSermon`, `deletefromsubsplash`, list lifecycle callables).
    - Handle `SUBSPLASH_LOCK_BUSY` details with clear, actionable user messaging and explicit retry action.

    Preserve locked decision:
    - Retry remains caller-controlled (no automatic hidden retries).
  </action>
  <verify>
    <automated>pnpm lint && pnpm build-functions && pnpm build</automated>
  </verify>
  <done>
    Admin mutation callers consistently send operation keys and handle lock contention with explicit, actionable retry behavior.
  </done>
</task>

</tasks>

<verification>
- Root lint/build and functions build pass after caller contract updates.
- Shared helper tests pass for operation-key generation and busy-detail parsing.
- No migrated mutation path still relies on generic lock contention alerts without retry metadata.
</verification>

<success_criteria>
- End-to-end caller paths use the phase lock/idempotency contract.
- Busy contention is surfaced with consistent user guidance and no hidden retries.
- Operation-key generation and parsing logic are centralized and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-05-SUMMARY.md`
</output>
