---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - functions/src/uploadToSubsplash.ts
  - functions/src/editSubsplashSermon.ts
  - functions/src/deleteFromSubsplash.ts
  - functions/src/test/series/mocks.ts
  - functions/src/test/soundcloud/mocks.ts
  - functions/src/test/subsplash/uploadToSubsplash.locking.test.ts
  - functions/src/test/subsplash/editSubsplashSermon.locking.test.ts
  - functions/src/test/subsplash/deleteFromSubsplash.locking.test.ts
autonomous: true
requirements:
  - LOCK-02
  - LOCK-03
  - LOCK-04
user_setup: []
must_haves:
  truths:
    - "Sermon/media-item mutation callables serialize conflicting writes on the same media item."
    - "Sermon mutation lock contention yields standard busy payload and caller-controlled retry metadata."
    - "Retrying sermon/media mutations with the same operation key avoids duplicate external side-effects."
    - "Legacy string-return error paths are removed from these mutation callables."
  artifacts:
    - path: "functions/src/uploadToSubsplash.ts"
      provides: "Lock + idempotency wrapped media upload mutation callable."
      min_lines: 80
    - path: "functions/src/editSubsplashSermon.ts"
      provides: "Lock-guarded media edit callable with structured errors."
    - path: "functions/src/deleteFromSubsplash.ts"
      provides: "Lock-guarded media delete callable with replay-safe operation keys."
    - path: "functions/src/test/subsplash/uploadToSubsplash.locking.test.ts"
      provides: "Coverage for lock contention and operation-key replay in upload path."
  key_links:
    - from: "functions/src/uploadToSubsplash.ts"
      to: "functions/src/locks/withSubsplashLocks.ts"
      via: "media-item lock before create/transcode/media-item calls"
      pattern: "withSubsplashLocks"
    - from: "functions/src/editSubsplashSermon.ts"
      to: "functions/src/locks/withIdempotency.ts"
      via: "operation-key replay"
      pattern: "withIdempotency"
    - from: "functions/src/deleteFromSubsplash.ts"
      to: "firebase-functions/v2/https HttpsError.details"
      via: "standardized contention and error contract"
      pattern: "new HttpsError"
---

<objective>
Migrate sermon/media-item mutation callables to the distributed lock and idempotency contract.

Purpose: prevent concurrent overwrite races on Subsplash media-item mutations and normalize callable error behavior.
Output: lock-guarded sermon/media callables plus focused lock/idempotency tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-RESEARCH.md
@functions/src/uploadToSubsplash.ts
@functions/src/editSubsplashSermon.ts
@functions/src/deleteFromSubsplash.ts

<interfaces>
From functions/src/uploadToSubsplash.ts:
```typescript
export interface UPLOAD_TO_SUBSPLASH_INCOMING_DATA {
  title: string;
  subtitle: string;
  speakers: ISpeaker[];
  autoPublish: boolean;
  audioTitle: string;
  audioUrl: string;
}
```

From functions/src/editSubsplashSermon.ts:
```typescript
export interface EDIT_SUBSPLASH_SERMON_INCOMING_DATA {
  subsplashId: string;
}
```

From functions/src/deleteFromSubsplash.ts:
```typescript
export interface DeleteFromSubsplashInputType {
  subsplashId: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize sermon/media callable input and error contracts for lock integration</name>
  <files>functions/src/uploadToSubsplash.ts, functions/src/editSubsplashSermon.ts, functions/src/deleteFromSubsplash.ts</files>
  <behavior>
    - Test 1: Missing/invalid auth and input produce `HttpsError`, not string/object sentinel returns.
    - Test 2: Each callable accepts and validates an operation key field used by idempotency wrapper.
    - Test 3: Lock contention uses standard busy payload details contract.
  </behavior>
  <action>
    Update the three sermon/media callables to a uniform mutation envelope:
    - Add required `operationKey`.
    - Add required lock target key input when `subsplashId` is not yet known at request start.
    - Replace string/object error returns with `HttpsError` throws to preserve typed error handling.

    Keep backwards compatibility where practical:
    - Preserve existing success payload shapes.
    - Preserve current auth role checks.
  </action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    Sermon/media callables share a consistent input and error contract compatible with lock/idempotency wrappers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply lock + idempotency wrappers to sermon/media mutation paths</name>
  <files>functions/src/uploadToSubsplash.ts, functions/src/editSubsplashSermon.ts, functions/src/deleteFromSubsplash.ts</files>
  <behavior>
    - Test 1: Concurrent upload/edit/delete for same media-item key cannot execute side-effects simultaneously.
    - Test 2: Retries with same operation key replay terminal success result.
    - Test 3: Lock wait timeout returns busy payload with `wait_ms` and `retry_after_ms`.
  </behavior>
  <action>
    Wrap each callable with `withSubsplashLocks` and `withIdempotency`.

    Required lock scope:
    - `uploadToSubsplash`: `media-item:{request.lockKey}` until real Subsplash ID exists.
    - `editSubsplashSermon`: `media-item:{subsplashId}`
    - `deleteFromSubsplash`: `media-item:{subsplashId}`

    Enforce read-after-lock:
    - Any metadata reads that determine remote mutation payload happen inside lock callback.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/subsplash/uploadToSubsplash.locking.test.ts src/test/subsplash/editSubsplashSermon.locking.test.ts src/test/subsplash/deleteFromSubsplash.locking.test.ts"</automated>
  </verify>
  <done>
    Sermon/media mutation callables are lock-guarded and idempotent with standard contention signaling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add lock/idempotency regression suites for sermon/media callables</name>
  <files>functions/src/test/subsplash/uploadToSubsplash.locking.test.ts, functions/src/test/subsplash/editSubsplashSermon.locking.test.ts, functions/src/test/subsplash/deleteFromSubsplash.locking.test.ts, functions/src/test/series/mocks.ts, functions/src/test/soundcloud/mocks.ts</files>
  <behavior>
    - Test 1: Busy-contention error details are asserted for each callable.
    - Test 2: Operation-key replay path is asserted for upload/edit/delete.
    - Test 3: Existing mock infrastructure remains deterministic with new wrapper boundaries.
  </behavior>
  <action>
    Add focused tests for lock contention and idempotent replay in sermon/media callables. Reuse existing mock patterns and only extend shared mocks where lock wrappers require additional stubbing.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/subsplash"</automated>
  </verify>
  <done>
    Sermon/media lock guarantees are enforced by automated regression tests and isolated from unrelated suites.
  </done>
</task>

</tasks>

<verification>
- New `src/test/subsplash/*locking.test.ts` suite passes under emulator execution.
- Error paths now consistently throw `HttpsError` with structured details.
- Callable type updates compile across the functions package.
</verification>

<success_criteria>
- Upload/edit/delete sermon/media-item callables follow the same lock/idempotency contract as other mutation surfaces.
- Busy contention contract is standardized across all sermon/media mutation endpoints.
- No string-return error paths remain in migrated mutation callables.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-04-SUMMARY.md`
</output>
