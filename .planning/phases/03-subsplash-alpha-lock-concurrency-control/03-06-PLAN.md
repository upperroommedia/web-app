---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/bulkAddToSeries.ts
  - functions/src/test/series/bulkAddToSeries.test.ts
  - functions/src/test/series/bulkAddToSeries.locking.test.ts
  - functions/src/test/series/bulkAddToSeries.crossCallableLocking.test.ts
  - pages/admin/series/[seriesId].tsx
  - utils/callableConcurrency.ts
  - utils/callableConcurrency.test.ts
autonomous: true
gap_closure: true
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
  - LOCK-04
  - LOCK-05
user_setup: []
must_haves:
  truths:
    - "bulkAddToSeries acquires deterministic series/media-item locks before any membership read that drives writes."
    - "bulkAddToSeries retries are idempotent via stable operation keys and replay prior terminal results."
    - "Stale caller order snapshots are rejected with failed-precondition instead of overwriting newer order."
    - "Bulk publish payload/result bounds prevent idempotency persistence failures for large batches."
    - "Series detail bulk publish caller sends operationKey to bulkaddtoseries, reuses the same key for explicit retry of the same intent, and keeps retry user-triggered."
  artifacts:
    - path: "functions/src/bulkAddToSeries.ts"
      provides: "Lock + idempotency envelope around bulk membership/reorder mutations with stale-snapshot guard and bounded payloads."
      contains: "withIdempotency"
    - path: "functions/src/test/series/bulkAddToSeries.locking.test.ts"
      provides: "Regression coverage for lock wrapper usage and contention contract."
    - path: "functions/src/test/series/bulkAddToSeries.crossCallableLocking.test.ts"
      provides: "Regression coverage for contention against delete/reorder callables sharing series/media lock keys."
    - path: "pages/admin/series/[seriesId].tsx"
      provides: "bulkaddtoseries caller payload includes stable retry operationKey and expected membership snapshot hash."
    - path: "functions/src/test/series/bulkAddToSeries.test.ts"
      provides: "Behavioral baseline coverage remains green with new operationKey + stale-snapshot contract."
    - path: "utils/callableConcurrency.ts"
      provides: "Shared helper for stable intent-based operation keys used across explicit retries."
  key_links:
    - from: "functions/src/bulkAddToSeries.ts"
      to: "functions/src/locks/withSubsplashLocks.ts"
      via: "series/media-item lock scope wrapper"
      pattern: "withSubsplashLocks"
    - from: "functions/src/bulkAddToSeries.ts"
      to: "functions/src/locks/withIdempotency.ts"
      via: "operation-key claim/complete/fail envelope"
      pattern: "withIdempotency"
    - from: "pages/admin/series/[seriesId].tsx"
      to: "bulkaddtoseries callable"
      via: "operationKey + expected snapshot hash propagation"
      pattern: "operationKey"
    - from: "functions/src/bulkAddToSeries.ts"
      to: "functions/src/deleteSeries.ts"
      via: "shared `series:{id}` lock key must serialize cross-callable mutations"
      pattern: "series:"
---

<objective>
Close the remaining backend concurrency gap by migrating `bulkAddToSeries` to the same lock/idempotency contract already used by other phase-03 mutation callables.

Purpose: eliminate the final unlocked read-before-write path so stale concurrent operations cannot duplicate side effects or reorder with stale membership.
Output: lock-scoped + idempotent `bulkAddToSeries` implementation, updated series detail caller contract, and focused regression tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-VERIFICATION.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-05-SUMMARY.md
@functions/src/bulkAddToSeries.ts
@functions/src/test/series/bulkAddToSeries.test.ts
@functions/src/locks/withSubsplashLocks.ts
@functions/src/locks/withIdempotency.ts
@pages/admin/series/[seriesId].tsx

<interfaces>
From functions/src/bulkAddToSeries.ts:
```typescript
export interface BulkAddToSeriesInputType {
  firestoreSeriesId: string;
  seriesSubsplashId: string;
  operationKey: string;
  expectedPublishedMembershipHash: string;
  adds: Array<{ mediaItemId: string; sermonId?: string }>;
  publishedItemOrder: string[];
  maxConcurrency?: number;
  rollbackOnFailure?: boolean;
}
```

From functions/src/locks/withSubsplashLocks.ts:
```typescript
export const withSubsplashLocks = async <T>(
  lockKeys: string[],
  run: () => Promise<T>,
  options: { operationKey?: string; waitTimeoutMs?: number; ... }
): Promise<T>;
```

From functions/src/locks/withIdempotency.ts:
```typescript
export const withIdempotency = async <T>(
  operationKey: string,
  run: () => Promise<T>
): Promise<T>;
```

From utils/callableConcurrency.ts:
```typescript
export const createOperationKey = (scope: string, entityId: string): string;
export const createRetryIntentKey = (scope: string, entityId: string, intentFingerprint: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing lock/idempotency contract tests for bulkAddToSeries</name>
  <files>functions/src/test/series/bulkAddToSeries.test.ts, functions/src/test/series/bulkAddToSeries.locking.test.ts, functions/src/test/series/bulkAddToSeries.crossCallableLocking.test.ts</files>
  <behavior>
    - Test 1: Callable rejects missing/blank `operationKey` with `invalid-argument`.
    - Test 2: Duplicate `operationKey` replays terminal result without duplicate patch/reorder side effects.
    - Test 3: Lock contention paths surface `SUBSPLASH_LOCK_BUSY` structured details (`locked_keys`, `wait_ms`, `retry_after_ms`).
    - Test 4: Request with stale `expectedPublishedMembershipHash` fails with `failed-precondition` and does not call reorder writes.
    - Test 5: Cross-callable contention (`bulkAddToSeries` vs `deleteSeries` or `reorderSeriesItems`) serializes on shared lock keys.
    - Test 6: Oversized bulk request/result is rejected or compacted before idempotency persistence exceeds datastore document limits.
  </behavior>
  <action>
    Extend the existing bulk-add series tests and add dedicated locking + cross-callable contention suites to codify the verification gaps before implementation.
    Keep fixtures aligned with existing series mocks and maintain deterministic expectations for side-effect counts.
  </action>
  <verify>
    <automated>cd functions && pnpm exec jest --runInBand --forceExit src/test/series/bulkAddToSeries.test.ts src/test/series/bulkAddToSeries.locking.test.ts src/test/series/bulkAddToSeries.crossCallableLocking.test.ts</automated>
  </verify>
  <done>
    Tests fail first against current implementation and explicitly capture required lock/idempotency behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate bulkAddToSeries to lock + idempotency envelope with stale-snapshot and payload safety guards</name>
  <files>functions/src/bulkAddToSeries.ts, pages/admin/series/[seriesId].tsx</files>
  <behavior>
    - Test 1: All remote membership reads and patch/reorder writes execute inside deterministic lock scope (`series` + sorted `media-item` keys).
    - Test 2: Callable wraps mutation execution in `withIdempotency(operationKey, ...)` and returns replayed results for duplicates.
    - Test 3: Callable validates `expectedPublishedMembershipHash`; stale snapshots fail before reorder with actionable `failed-precondition`.
    - Test 4: Callable enforces bounded bulk input/output so idempotency persistence stays under Firestore document limits.
  </behavior>
  <action>
    Update `BulkAddToSeriesInputType` and callable validation to require `operationKey` and `expectedPublishedMembershipHash`, then wrap the mutation body with `withIdempotency` outside `withSubsplashLocks`.
    Build lock keys from `seriesSubsplashId` and all participating media item IDs using deterministic ordering; run `fetchRemoteSeriesMembership`, add/rollback mutations, and final reorder entirely inside the lock callback.
    Compute canonical membership hash from the locked remote membership and compare with caller `expectedPublishedMembershipHash`; on mismatch, abort with structured precondition error so user can refresh and retry.
    Add explicit safety bounds (for example max adds per request and capped replay payload shape) so result persistence in idempotency docs cannot exceed storage limits.
    Keep lock wait behavior and busy payload contract unchanged.
  </action>
  <verify>
    <automated>cd functions && pnpm exec jest --runInBand --forceExit src/test/series/bulkAddToSeries.test.ts src/test/series/bulkAddToSeries.locking.test.ts src/test/series/bulkAddToSeries.crossCallableLocking.test.ts</automated>
  </verify>
  <done>
    `bulkAddToSeries` follows the lock/idempotency substrate with stale-snapshot protection and bounded payload safety.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add stable retry operation key strategy in series detail caller</name>
  <files>pages/admin/series/[seriesId].tsx, utils/callableConcurrency.ts, utils/callableConcurrency.test.ts</files>
  <behavior>
    - Test 1: Same explicit retry intent (same selection + order fingerprint) reuses the same operation key.
    - Test 2: Different mutation intent generates a different operation key.
    - Test 3: Caller sends both `operationKey` and `expectedPublishedMembershipHash` to `bulkaddtoseries`; no hidden auto-retry loop is introduced.
  </behavior>
  <action>
    Add helper support for intent-fingerprint-based retry keys.
    In `pages/admin/series/[seriesId].tsx`, derive an intent fingerprint from selected sermon IDs + published order snapshot and reuse the same operation key for manual retries of that intent until terminal success or intent change.
    Include `expectedPublishedMembershipHash` in the callable payload to pair client snapshot intent with server stale-state detection.
    Honor locked decisions: caller-controlled retries only; on contention, rely on structured busy payload rather than hidden retries.
  </action>
  <verify>
    <automated>pnpm exec jest --runInBand --forceExit utils/callableConcurrency.test.ts && cd functions && pnpm exec jest --runInBand --forceExit src/test/series/bulkAddToSeries.test.ts src/test/series/bulkAddToSeries.locking.test.ts src/test/series/bulkAddToSeries.crossCallableLocking.test.ts</automated>
  </verify>
  <done>
    Series detail caller uses stable intent-scoped operation keys and snapshot hash propagation without changing retry ownership.
  </done>
</task>

</tasks>

<verification>
- Bulk-add series tests (including cross-callable contention) pass with the new operation-key contract and lock/idempotency behavior.
- Lock contention remains surfaced as structured `SUBSPLASH_LOCK_BUSY` details.
- Stale snapshot requests fail with precondition guidance rather than overwriting newer order.
- Series detail bulk publish payload includes stable retry `operationKey` and `expectedPublishedMembershipHash`.
</verification>

<success_criteria>
- Verification gap "bulkAddToSeries unlocked mutation path" is closed.
- Verification gap "bulkAddToSeries missing idempotency contract" is closed.
- Cross-callable lock contention with series mutations is regression-tested.
- No stale read-before-write path remains in this callable.
- Replay payload safety prevents idempotency doc-overflow failures for large bulk actions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-06-SUMMARY.md`
</output>
