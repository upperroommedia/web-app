---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - functions/src/addToSeries.ts
  - functions/src/removeFromSeries.ts
  - functions/src/reorderSeriesItems.ts
  - functions/src/createSeries.ts
  - functions/src/deleteSeries.ts
  - functions/src/test/series/addToSeries.test.ts
  - functions/src/test/series/removeFromSeries.test.ts
  - functions/src/test/series/reorderSeriesItems.test.ts
  - functions/src/test/series/createSeries.test.ts
  - functions/src/test/series/deleteSeries.test.ts
autonomous: true
requirements:
  - LOCK-02
  - LOCK-03
  - LOCK-04
user_setup: []
must_haves:
  truths:
    - "Series mutations no longer execute concurrent side-effects on the same series/media-item."
    - "Series mutation callables return structured lock-busy payloads after bounded wait."
    - "Retrying series mutations with same operation key is idempotent."
    - "All read-before-write decisions in series callables happen after lock acquisition."
  artifacts:
    - path: "functions/src/addToSeries.ts"
      provides: "Series assignment mutation guarded by lock + idempotency."
    - path: "functions/src/reorderSeriesItems.ts"
      provides: "Read-after-lock reorder flow for remote series membership."
    - path: "functions/src/createSeries.ts"
      provides: "Series lifecycle creation path with lock/idempotency integration."
    - path: "functions/src/deleteSeries.ts"
      provides: "Series deletion guarded by lock lifecycle."
    - path: "functions/src/test/series/reorderSeriesItems.test.ts"
      provides: "Regression coverage for lock contention and idempotent retries."
  key_links:
    - from: "functions/src/addToSeries.ts"
      to: "functions/src/locks/withSubsplashLocks.ts"
      via: "series + media-item lock acquisition before patch"
      pattern: "withSubsplashLocks"
    - from: "functions/src/reorderSeriesItems.ts"
      to: "functions/src/helpers/seriesHelpers.ts"
      via: "getSeriesItems read-after-lock then patchSeriesItemPositions"
      pattern: "getSeriesItems|patchSeriesItemPositions"
    - from: "functions/src/createSeries.ts"
      to: "functions/src/locks/withIdempotency.ts"
      via: "operation-key claim and replay"
      pattern: "withIdempotency"
---

<objective>
Migrate all series-domain mutation callables to the new lock + idempotency infrastructure from plan 03-01.

Purpose: prevent stale reads and concurrent overwrite races across series assignment, reorder, create, and delete paths.
Output: lock-guarded series callables with standardized busy payloads and updated regression tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-RESEARCH.md
@functions/src/addToSeries.ts
@functions/src/removeFromSeries.ts
@functions/src/reorderSeriesItems.ts
@functions/src/createSeries.ts
@functions/src/deleteSeries.ts
@functions/src/helpers/seriesHelpers.ts

<interfaces>
From functions/src/addToSeries.ts:
```typescript
export interface AddToSeriesInputType {
  seriesSubsplashId: string;
  mediaItemId: string;
  position?: number;
}
```

From functions/src/reorderSeriesItems.ts:
```typescript
export interface ReorderSeriesItemsInputType {
  firestoreSeriesId: string;
  itemOrder: { mediaItemId: string; position: number }[];
}
```

From functions/src/createSeries.ts:
```typescript
export interface CreateSeriesInputType {
  title: string;
  summary?: string;
  ownerId: string;
  skipSubsplash?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock-guard series membership and reorder callables</name>
  <files>functions/src/addToSeries.ts, functions/src/removeFromSeries.ts, functions/src/reorderSeriesItems.ts</files>
  <behavior>
    - Test 1: Two concurrent `addToSeries` calls for same media item do not both patch Subsplash.
    - Test 2: Reorder reads remote membership only after lock acquisition and fails with busy payload on timeout.
    - Test 3: Duplicate `operationKey` on add/remove/reorder replays prior terminal result.
  </behavior>
  <action>
    Integrate `withSubsplashLocks` and `withIdempotency` into the three series membership callables.

    Required lock scope:
    - `addToSeries`: `series:{seriesSubsplashId}` + `media-item:{mediaItemId}`
    - `removeFromSeries`: `media-item:{mediaItemId}`
    - `reorderSeriesItems`: `series:{firestoreSeriesId}`

    Critical constraints:
    - Preserve caller-controlled retry contract (no internal retry loop beyond bounded lock wait).
    - Ensure all reads that inform writes (Firestore series lookup, Subsplash membership fetch) run inside lock callback.
    - Return busy contention via standard details payload from plan 03-01 helpers.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/series/addToSeries.test.ts src/test/series/removeFromSeries.test.ts src/test/series/reorderSeriesItems.test.ts"</automated>
  </verify>
  <done>
    Series add/remove/reorder callables are lock-guarded, idempotent by operation key, and emit standard busy payloads.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock-guard series lifecycle create/delete callables</name>
  <files>functions/src/createSeries.ts, functions/src/deleteSeries.ts</files>
  <behavior>
    - Test 1: Concurrent delete requests for same series cannot interleave external and Firestore side-effects.
    - Test 2: Create/delete retry with identical operation key does not duplicate series mutation side-effects.
    - Test 3: Failures still release lock in finally path.
  </behavior>
  <action>
    Apply the same lock/idempotency wrapper contract to series lifecycle callables.

    Required lock keys:
    - `createSeries`: `series:{generatedFirestoreId}` (create ID first, then lock before remote/local writes)
    - `deleteSeries`: `series:{firestoreId}`

    Implementation requirements:
    - Keep existing auth and role checks.
    - Keep current local-only `skipSubsplash` behavior, but still wrap mutation path with idempotency.
    - Ensure cleanup batch operations remain inside lock scope for delete.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/series/createSeries.test.ts src/test/series/deleteSeries.test.ts"</automated>
  </verify>
  <done>
    Series create/delete paths follow the same lock and idempotency guarantees as membership mutations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend series tests for busy payload and idempotency replay</name>
  <files>functions/src/test/series/addToSeries.test.ts, functions/src/test/series/removeFromSeries.test.ts, functions/src/test/series/reorderSeriesItems.test.ts, functions/src/test/series/createSeries.test.ts, functions/src/test/series/deleteSeries.test.ts</files>
  <behavior>
    - Test 1: Busy contention errors include required details fields.
    - Test 2: Replayed operation key returns stable result payload.
    - Test 3: Lock timeout path is deterministic at 10,000 ms wait budget.
  </behavior>
  <action>
    Add focused test cases proving lock contention and operation-key replay for every modified series callable.

    Keep tests deterministic:
    - Use emulator-safe fake timers or mocked clock where needed.
    - Reuse existing series mocks; do not introduce network-dependent test behavior.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/series"</automated>
  </verify>
  <done>
    Series suite prevents regressions on lock-wait, busy payload schema, and idempotent retry semantics.
  </done>
</task>

</tasks>

<verification>
- Series callable tests pass under Firestore + RTDB emulator execution.
- Busy contention payload schema stays consistent across all series mutation paths.
- Type-check confirms updated callable input interfaces compile with existing callers.
</verification>

<success_criteria>
- All series mutation callables use lock acquisition/release owned by Cloud Functions.
- Series read-after-lock rule is enforced in code paths that decide writes.
- Operation-key idempotency is uniformly active for series mutations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-02-SUMMARY.md`
</output>
