---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - functions/src/addToList.ts
  - functions/src/removeFromList.ts
  - functions/src/createNewSubsplashList.ts
  - functions/src/editSubsplashList.ts
  - functions/src/deleteSubsplashList.ts
  - functions/src/test/addToList/concurrentAccess.test.ts
  - functions/src/test/addToList/transactionRetryDuplication.test.ts
  - functions/src/test/addToList/transactionRetryInconsistency.test.ts
  - functions/src/test/removeFromList/basic.test.ts
autonomous: true
requirements:
  - LOCK-02
  - LOCK-03
  - LOCK-04
user_setup: []
must_haves:
  truths:
    - "List mutation callables serialize conflicting writes on the same list or media item."
    - "Lock contention on list mutations returns standard busy metadata after bounded wait."
    - "List mutation retries with same operation key do not duplicate side-effects."
    - "Read decisions that drive list writes happen after lock acquisition."
  artifacts:
    - path: "functions/src/addToList.ts"
      provides: "Lock/idempotency guarded add-to-list mutation flow."
      min_lines: 120
    - path: "functions/src/removeFromList.ts"
      provides: "Lock-guarded remove flow across overflow-chain lookup."
    - path: "functions/src/createNewSubsplashList.ts"
      provides: "List create callable using idempotent mutation envelope."
    - path: "functions/src/test/addToList/transactionRetryInconsistency.test.ts"
      provides: "Regression coverage for transaction-retry inconsistencies with lock/idempotency layer."
  key_links:
    - from: "functions/src/addToList.ts"
      to: "functions/src/locks/withSubsplashLocks.ts"
      via: "list/media-item lock acquisition"
      pattern: "withSubsplashLocks"
    - from: "functions/src/addToList.ts"
      to: "functions/src/locks/withIdempotency.ts"
      via: "operation-key replay for retries"
      pattern: "withIdempotency"
    - from: "functions/src/removeFromList.ts"
      to: "functions/src/helpers/addToListHelpers.ts"
      via: "overflow chain reads after lock"
      pattern: "getFullListRows"
---

<objective>
Migrate list-domain mutation callables to distributed lock + idempotency guarantees.

Purpose: eliminate list mutation race conditions and retry duplication risks while preserving current list/overflow behavior.
Output: lock-guarded list callables and updated list-focused regression suites.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-RESEARCH.md
@functions/src/addToList.ts
@functions/src/removeFromList.ts
@functions/src/createNewSubsplashList.ts
@functions/src/editSubsplashList.ts
@functions/src/deleteSubsplashList.ts
@functions/src/test/addToList/transactionRetryInconsistency.test.ts
@functions/src/test/addToList/transactionRetryDuplication.test.ts

<interfaces>
From functions/src/addToList.ts:
```typescript
export interface AddtoListInputType {
  destinationListIds: string[];
  mediaItem: SubsplashMediaItem;
  maxListSize?: number;
}
```

From functions/src/removeFromList.ts:
```typescript
export interface RemoveFromListInputType {
  listIds: string[];
  listItemIds: string[];
  itemIds: string[];
  itemTypes: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock-guard add/remove list-item mutations</name>
  <files>functions/src/addToList.ts, functions/src/removeFromList.ts</files>
  <behavior>
    - Test 1: Concurrent `addToList` against same list/media-item pair no longer duplicates downstream side-effects.
    - Test 2: Concurrent remove requests for same list item serialize through lock ownership.
    - Test 3: Duplicate `operationKey` returns replayed result payload, not repeated write work.
  </behavior>
  <action>
    Integrate `withSubsplashLocks` and `withIdempotency` into list-item mutation paths.

    Lock scope rules:
    - `addToList`: all target `list:{listId}` keys plus `media-item:{mediaItem.id}`.
    - `removeFromList`: each target `list:{listId}` key plus `media-item:{itemId}` when type is `media-item`.

    Read-after-lock requirements:
    - Any call to fetch list rows / overflow state used to decide writes must execute inside lock callback.
    - Keep current overflow behavior (`CREATENEWLIST`, `REMOVEOLDEST`) unchanged.

    Safety:
    - Preserve partial success semantics for multi-list requests.
    - Keep existing auth and validation contract.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/addToList/concurrentAccess.test.ts src/test/removeFromList/basic.test.ts"</automated>
  </verify>
  <done>
    add/remove list-item callables are lock-guarded, idempotent, and maintain current output semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock-guard list lifecycle create/edit/delete callables</name>
  <files>functions/src/createNewSubsplashList.ts, functions/src/editSubsplashList.ts, functions/src/deleteSubsplashList.ts</files>
  <behavior>
    - Test 1: Edit/delete calls for same list ID contend on one list lock key.
    - Test 2: Retry with same operation key on create/edit/delete does not duplicate remote mutations.
    - Test 3: Busy payload schema matches standard contract on timeout.
  </behavior>
  <action>
    Add lock + idempotency wrappers to list lifecycle callables:
    - `editSubsplashList` and `deleteSubsplashList` use `list:{listId}`.
    - `createNewSubsplashList` uses deterministic creation key (title-derived lock key) to prevent duplicate concurrent creates for same logical list request.

    Keep behavior stable:
    - Do not change successful response payload shape.
    - Continue throwing `HttpsError` for auth/validation failures.
  </action>
  <verify>
    <automated>cd functions && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    List create/edit/delete callables follow the same lock/idempotency contract as list-item mutations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend list regression suites for contention and retry replay</name>
  <files>functions/src/test/addToList/concurrentAccess.test.ts, functions/src/test/addToList/transactionRetryDuplication.test.ts, functions/src/test/addToList/transactionRetryInconsistency.test.ts, functions/src/test/removeFromList/basic.test.ts</files>
  <behavior>
    - Test 1: Busy contention details are asserted in lock timeout paths.
    - Test 2: Transaction-retry duplication and inconsistency suites remain green with lock/idempotency wrappers.
    - Test 3: Same operation key produces deterministic replay output.
  </behavior>
  <action>
    Update list tests to prove the new concurrency contract while preserving existing business behaviors.

    Prioritize currently risky suites:
    - `transactionRetryDuplication`
    - `transactionRetryInconsistency`
    - `concurrentAccess`
    - remove-from-list overflow chain checks
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/addToList/transactionRetryDuplication.test.ts src/test/addToList/transactionRetryInconsistency.test.ts src/test/addToList/concurrentAccess.test.ts src/test/removeFromList/basic.test.ts"</automated>
  </verify>
  <done>
    List test suite enforces lock contention handling and idempotent retries for core list mutation workflows.
  </done>
</task>

</tasks>

<verification>
- Target add/remove list suites pass with Firestore + RTDB emulators.
- Lock-busy payload shape is consistent with plan 03-01 helper output.
- Existing overflow semantics remain unchanged in regression tests.
</verification>

<success_criteria>
- All targeted list mutation callables acquire list/media-item locks before mutating.
- Retried list mutation operations are safe and idempotent.
- Known addToList concurrency regression surfaces are covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-03-SUMMARY.md`
</output>
