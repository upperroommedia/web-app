---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 07
type: execute
wave: 2
depends_on:
  - 03-06
files_modified:
  - pages/admin/series.tsx
  - pages/admin/sermons.tsx
  - utils/deleteSermonWithExternalCleanup.ts
  - utils/deleteSermonWithExternalCleanup.test.ts
  - utils/callableConcurrency.contract.test.ts
  - utils/callableConcurrency.ts
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
  - LOCK-04
  - LOCK-05
user_setup: []
must_haves:
  truths:
    - "Admin series-list delete caller sends operationKey and surfaces lock-busy retry guidance."
    - "Sermon external cleanup utility propagates operationKey to deletefromsubsplash and blocks local delete when external cleanup fails or contends."
    - "Lock contention/error metadata is preserved from callable error details through utility and UI messaging (no generic masking)."
    - "LOCK-01..LOCK-05 definitions and traceability rows exist in REQUIREMENTS.md."
  artifacts:
    - path: "pages/admin/series.tsx"
      provides: "Series delete caller contract with operationKey + lock-busy messaging."
    - path: "pages/admin/sermons.tsx"
      provides: "Sermon delete UX surfaces explicit lock-busy retry guidance from cleanup utility details."
    - path: "utils/deleteSermonWithExternalCleanup.ts"
      provides: "Cleanup operation-key propagation and contention-aware failure handling without swallowing structured details."
    - path: "utils/deleteSermonWithExternalCleanup.test.ts"
      provides: "Automated regression coverage for operationKey propagation, failure blocking semantics, and busy-detail preservation."
    - path: "utils/callableConcurrency.contract.test.ts"
      provides: "Contract assertions for remaining caller operation-key adoption."
    - path: "utils/callableConcurrency.ts"
      provides: "Shared formatter/parser helpers used to preserve busy-detail guidance across utility + UI boundaries."
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical LOCK requirement definitions and traceability mapping."
  key_links:
    - from: "pages/admin/series.tsx"
      to: "deleteseries callable"
      via: "operationKey payload + parseLockBusyDetails retry guidance"
      pattern: "createOperationKey"
    - from: "utils/deleteSermonWithExternalCleanup.ts"
      to: "deletefromsubsplash callable"
      via: "operationKey propagation and busy-contention handling"
      pattern: "operationKey"
    - from: "pages/admin/sermons.tsx"
      to: "utils/deleteSermonWithExternalCleanup.ts"
      via: "busy/error details surfaced as actionable retry guidance in toast"
      pattern: "retry_after_ms|parseLockBusyDetails"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 03 roadmap requirements"
      via: "LOCK-01..LOCK-05 definition + traceability rows"
      pattern: "LOCK-0[1-5]"
---

<objective>
Close the remaining caller-contract and documentation verification gaps for Phase 03.

Purpose: make all remaining admin deletion entry points conform to operation-key + lock-busy guidance expectations and restore formal requirement traceability.
Output: caller updates for series list + sermon cleanup flows, regression tests, and LOCK requirement definitions/traceability rows in REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-VERIFICATION.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-05-SUMMARY.md
@pages/admin/series.tsx
@utils/deleteSermonWithExternalCleanup.ts
@utils/callableConcurrency.ts
@functions/src/deleteSeries.ts
@functions/src/deleteFromSubsplash.ts
@.planning/REQUIREMENTS.md

<interfaces>
From functions/src/deleteSeries.ts:
```typescript
export interface DeleteSeriesInputType {
  firestoreId: string;
  operationKey?: string;
}
```

From functions/src/deleteFromSubsplash.ts:
```typescript
export interface DeleteFromSubsplashInputType {
  operationKey: string;
  subsplashId: string;
}
```

From utils/callableConcurrency.ts:
```typescript
export const createOperationKey = (scope: string, entityId: string): string;
export const parseLockBusyDetails = (error: unknown): LockBusyDetails | null;
export const formatLockBusyRetryMessage = (fallbackMessage: string, details: LockBusyDetails): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing caller-contract tests for remaining deletion entry points</name>
  <files>utils/callableConcurrency.contract.test.ts, utils/deleteSermonWithExternalCleanup.test.ts</files>
  <behavior>
    - Test 1: `pages/admin/series.tsx` includes delete-series `operationKey` generation and lock-busy parsing usage.
    - Test 2: `deleteSermonWithExternalCleanup` passes `operationKey` to `deletefromsubsplash`.
    - Test 3: External cleanup contention/failure blocks local Firestore delete (no `Promise.allSettled` swallow path).
    - Test 4: Structured contention details (`code`, `locked_keys`, `retry_after_ms`) are preserved in thrown errors/messages for UI handling.
    - Test 5: `pages/admin/sermons.tsx` maps cleanup lock-busy details to explicit retry guidance in toast messaging.
  </behavior>
  <action>
    Extend the existing caller contract test to assert the series-list and sermon-list delete path contracts, and add a dedicated unit test file for `deleteSermonWithExternalCleanup`.
    Use deterministic mocks for callable wrappers so failure/lock-busy behavior is asserted without network dependencies.
  </action>
  <verify>
    <automated>pnpm exec jest --runInBand --forceExit utils/callableConcurrency.contract.test.ts utils/deleteSermonWithExternalCleanup.test.ts</automated>
  </verify>
  <done>
    Regression tests fail against current gaps and encode the expected caller contract behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement operationKey + contention handling in series-list and external cleanup callers</name>
  <files>pages/admin/series.tsx, pages/admin/sermons.tsx, utils/deleteSermonWithExternalCleanup.ts, utils/callableConcurrency.ts</files>
  <behavior>
    - Test 1: Series list delete callable payload includes `operationKey` built via `createOperationKey`.
    - Test 2: Series list delete errors parse lock-busy details and present retry guidance with `retry_after_ms`.
    - Test 3: Cleanup utility propagates `operationKey` to `deletefromsubsplash` and throws actionable lock-busy errors instead of logging-and-continuing.
    - Test 4: Utility preserves structured callable error details (including lock busy metadata) so UI can distinguish contention from generic failures.
    - Test 5: Sermon page toast uses lock-busy retry guidance for cleanup contention and does not claim successful deletion when external cleanup fails.
  </behavior>
  <action>
    In `pages/admin/series.tsx`, import `createOperationKey` and `parseLockBusyDetails`, add operation key to `deleteseries` payload, and update error handling to surface explicit lock-busy retry guidance (`retry_after_ms`, optionally `locked_keys`) while keeping retries user-triggered.
    In `utils/deleteSermonWithExternalCleanup.ts`, generate and pass an operation key when invoking `deletefromsubsplash`; replace `Promise.allSettled` swallow behavior with fail-fast semantics that block local delete if external cleanup fails.
    Preserve and rethrow structured error details (especially lock-busy metadata) using a typed utility error contract so calling UI can render actionable retry timing instead of generic text.
    In `pages/admin/sermons.tsx`, parse/format lock-busy details from cleanup failures into retry guidance toast copy.
    Add shared helper formatting in `utils/callableConcurrency.ts` to keep busy-message shape consistent across admin surfaces.
    Preserve existing non-contention success behavior and avoid auto-retry loops.
  </action>
  <verify>
    <automated>pnpm exec jest --runInBand --forceExit utils/callableConcurrency.contract.test.ts utils/deleteSermonWithExternalCleanup.test.ts</automated>
  </verify>
  <done>
    Remaining admin delete entry points follow the Phase 03 caller contract with explicit retry guidance and no silent contention suppression.
  </done>
</task>

<task type="auto">
  <name>Task 3: Restore LOCK requirement definitions and traceability coverage</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Add canonical requirement definitions for `LOCK-01` through `LOCK-05` under the current milestone requirements section, matching the Phase 03 goal/verification contract:
    - deterministic lock-before-read mutation sequencing,
    - bounded contention contract payload,
    - operation-key idempotency behavior,
    - enforced finally-path release + dead-letter logging,
    - caller operation-key propagation + lock-busy retry UX.
    Update the Traceability table with rows for each LOCK requirement mapped to Phase 03 and mark status based on current gap-closure completion target.
  </action>
  <verify>
    <automated>rg -n "LOCK-01|LOCK-02|LOCK-03|LOCK-04|LOCK-05" .planning/REQUIREMENTS.md && rg -n "\\| LOCK-0[1-5] \\|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md includes complete LOCK-01..LOCK-05 definitions and traceability entries, removing verification "requirements blocked" status.
  </done>
</task>

</tasks>

<verification>
- Caller contract tests pass for series list delete and sermon cleanup utility behavior.
- No remaining gap artifact omits operationKey where required by callable contract.
- Sermon delete flow no longer swallows external cleanup failures and preserves contention details for retry UX.
- REQUIREMENTS.md explicitly defines and traces LOCK-01..LOCK-05.
</verification>

<success_criteria>
- Verification gaps for `pages/admin/series.tsx` and `utils/deleteSermonWithExternalCleanup.ts` are closed.
- External cleanup contention is actionable instead of silent.
- Lock-busy details survive utility boundaries and are rendered as explicit retry guidance in UI.
- Phase 03 requirement traceability is complete and auditable in REQUIREMENTS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-07-SUMMARY.md`
</output>
