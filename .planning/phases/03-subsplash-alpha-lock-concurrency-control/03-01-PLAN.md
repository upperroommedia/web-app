---
phase: 03-subsplash-alpha-lock-concurrency-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/src/locks/lockTypes.ts
  - functions/src/locks/contentionError.ts
  - functions/src/locks/subsplashLockStore.ts
  - functions/src/locks/withSubsplashLocks.ts
  - functions/src/locks/idempotencyStore.ts
  - functions/src/locks/withIdempotency.ts
  - functions/src/locks/releaseFailureSink.ts
  - functions/src/test/locks/contentionError.test.ts
  - functions/src/test/locks/subsplashLockStore.test.ts
  - functions/src/test/locks/idempotencyStore.test.ts
  - functions/src/test/setup.ts
autonomous: true
requirements:
  - LOCK-01
  - LOCK-03
  - LOCK-04
  - LOCK-05
user_setup: []
must_haves:
  truths:
    - "Concurrent mutations on the same entity no longer run side-effects at the same time."
    - "Lock contention waits up to 10 seconds, then returns a structured busy payload."
    - "Retrying the same operation key replays stored success instead of duplicating side-effects."
    - "Orphaned lock-release failures are persisted for operator follow-up."
  artifacts:
    - path: "functions/src/locks/subsplashLockStore.ts"
      provides: "RTDB lease lock acquire/heartbeat/release with stale-lock takeover."
      min_lines: 80
    - path: "functions/src/locks/withSubsplashLocks.ts"
      provides: "Deterministic multi-lock orchestration (entity type order, then entity ID)."
      exports: ["withSubsplashLocks", "sortLockKeys"]
    - path: "functions/src/locks/idempotencyStore.ts"
      provides: "Operation-key claim/complete/replay persistence."
      exports: ["claimOperation", "completeOperation", "getOperationResult"]
    - path: "functions/src/locks/contentionError.ts"
      provides: "Standard `HttpsError` busy payload contract."
      exports: ["buildSubsplashLockBusyError"]
    - path: "functions/src/locks/releaseFailureSink.ts"
      provides: "Dead-letter sink for release failures."
  key_links:
    - from: "functions/src/locks/withSubsplashLocks.ts"
      to: "functions/src/locks/subsplashLockStore.ts"
      via: "acquire/wait/heartbeat/release lifecycle"
      pattern: "acquireWithWait|startHeartbeat|releaseLock"
    - from: "functions/src/locks/withIdempotency.ts"
      to: "functions/src/locks/idempotencyStore.ts"
      via: "claim -> execute -> complete/fail"
      pattern: "claimOperation|completeOperation|markOperationFailed"
    - from: "functions/src/locks/subsplashLockStore.ts"
      to: "functions/src/locks/releaseFailureSink.ts"
      via: "release failure fallback"
      pattern: "logLockReleaseFailure"
---

<objective>
Create the shared lock/idempotency substrate required by all Subsplash mutation callables in this phase.

Purpose: enforce non-negotiable phase decisions once, then reuse the same primitives across series/list/sermon callables.
Output: lock and idempotency modules, busy error contract, dead-letter logging path, and focused lock-layer tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-CONTEXT.md
@.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-RESEARCH.md
@firebase/firebaseAdmin.ts
@functions/src/handleError.ts

<interfaces>
From firebase/firebaseAdmin.ts:
```typescript
import * as firebaseAdmin from 'firebase-admin';
export default firebaseAdmin;
```

From functions/src/handleError.ts:
```typescript
const handleError = (error: unknown): HttpsError => {
  if (error instanceof HttpsError) return error;
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define lock contracts and contention payload helpers</name>
  <files>functions/src/locks/lockTypes.ts, functions/src/locks/contentionError.ts, functions/src/test/locks/contentionError.test.ts</files>
  <behavior>
    - Test 1: Busy error payload contains `code`, `locked_keys`, `wait_ms`, and `retry_after_ms`.
    - Test 2: Entity ordering is deterministic (`series`, `list`, `media-item`, then lexical entity ID).
    - Test 3: Default lock wait timeout is exactly 10,000 ms when caller does not override.
  </behavior>
  <action>
    Add foundational types/constants for lock keys, lock records, and idempotency record status. Implement a contention helper that builds `HttpsError('aborted', ...)` with the standard details contract from phase decisions.

    Required contract:
    - Use machine code `SUBSPLASH_LOCK_BUSY`.
    - Include `locked_keys`, `wait_ms`, and `retry_after_ms`.
    - Keep retry strategy caller-controlled by returning metadata only (no automatic server retries).
  </action>
  <verify>
    <automated>cd functions && pnpm exec jest --runInBand --forceExit src/test/locks/contentionError.test.ts</automated>
  </verify>
  <done>
    Shared lock/idempotency type contracts and busy-payload helper exist with tests proving schema and timeout defaults.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RTDB lock store with deterministic multi-lock orchestration</name>
  <files>functions/src/locks/subsplashLockStore.ts, functions/src/locks/withSubsplashLocks.ts, functions/src/locks/releaseFailureSink.ts, functions/src/test/locks/subsplashLockStore.test.ts, functions/src/test/setup.ts</files>
  <behavior>
    - Test 1: Second owner waits and times out at 10 seconds when first owner keeps the lease alive.
    - Test 2: Expired lease can be reclaimed by a new owner (stale lock recovery).
    - Test 3: Multi-lock acquisition always follows global order regardless of input order.
    - Test 4: Release failure is recorded to dead-letter sink without crashing process teardown.
  </behavior>
  <action>
    Implement RTDB-backed locking at `subsplashLocks/{entityType}:{entityId}` using transactional compare-and-set, lease expiry, and heartbeat refresh.

    Implementation requirements:
    - Acquire/wait/release lives server-side only.
    - Acquire order must be deterministic: entity type order (`series`, `list`, `media-item`) then entity ID.
    - Use lease TTL + heartbeat for stale lock reclamation.
    - Always release in `finally`; if release fails, emit structured dead-letter event (Cloud Logging plus Firestore fallback collection).

    Testing requirement:
    - Ensure test bootstrap sets `FIREBASE_DATABASE_EMULATOR_HOST` so lock tests never touch production RTDB.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/locks/subsplashLockStore.test.ts"</automated>
  </verify>
  <done>
    Lock acquisition, bounded wait, heartbeat, stale takeover, ordered multi-lock acquisition, and dead-letter release logging are all implemented and tested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement operation-key idempotency store and wrapper</name>
  <files>functions/src/locks/idempotencyStore.ts, functions/src/locks/withIdempotency.ts, functions/src/test/locks/idempotencyStore.test.ts</files>
  <behavior>
    - Test 1: First claim for an operation key succeeds; concurrent claim returns in-progress status.
    - Test 2: Completed operation key replays stored terminal result payload.
    - Test 3: Failed operation key stores failure metadata without replaying stale success.
  </behavior>
  <action>
    Build Firestore-backed idempotency primitives for mutation callables:
    - Claim operation key (single writer).
    - Record terminal success payload for deterministic replay.
    - Record failure metadata for observability and safe retries.

    Enforce wrapper contract:
    - Every wrapped callable receives non-empty `operationKey`.
    - Retries with same key never duplicate external side-effects.
    - Wrapper composes cleanly with `withSubsplashLocks`.
  </action>
  <verify>
    <automated>cd functions && firebase emulators:exec --only firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand --forceExit src/test/locks/idempotencyStore.test.ts"</automated>
  </verify>
  <done>
    Idempotency claim/replay/failure persistence works and is reusable by all mutation callables in later plans.
  </done>
</task>

</tasks>

<verification>
- `cd functions && pnpm exec tsc --noEmit` passes.
- Lock-layer tests pass under Firestore + RTDB emulators.
- Busy error payload shape is stable and documented in tests.
</verification>

<success_criteria>
- Reusable lock/idempotency primitives exist and are validated before callable migrations start.
- Lock store uses RTDB with stale-lock recovery and deterministic ordering.
- Busy/error contract and release-failure dead-letter behavior are codified in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-subsplash-alpha-lock-concurrency-control/03-01-SUMMARY.md`
</output>
