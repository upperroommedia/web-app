# Deferred Items

## Out-of-scope pre-existing lint blockers discovered during 03-05 verification

- `pages/admin/sermons.tsx:110` — `no-void` error (`Expected 'undefined' and instead saw 'void'`).
- `components/uploaderComponents/UploaderComponent.tsx:507` — `no-void` error (`Expected 'undefined' and instead saw 'void'`).

These files are outside the 03-05 plan scope and were not modified as part of this execution.
