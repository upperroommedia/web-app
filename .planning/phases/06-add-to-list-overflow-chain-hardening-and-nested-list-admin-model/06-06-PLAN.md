---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 06
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - functions/src/helpers/listOverflowChain.ts
  - scripts/backfill-list-overflow-metadata.ts
  - functions/src/test/lists/backfillListOverflowMetadata.test.ts
autonomous: true
requirements: [OFLOW-02, OFLOW-06]
must_haves:
  truths:
    - "Legacy list documents can be repaired/backfilled into the explicit metadata model without hand-editing Firestore."
    - "Backfill derives root ids, overflow depth, logical totals, and canonical continuation names for consistent chains."
    - "Inconsistent chains are reported and skipped instead of being force-written into a worse state."
  artifacts:
    - path: "scripts/backfill-list-overflow-metadata.ts"
      provides: "One-off repair/backfill tool for legacy root/overflow docs and logical totals"
    - path: "functions/src/test/lists/backfillListOverflowMetadata.test.ts"
      provides: "Regression coverage for metadata repair, dry-run output, and inconsistency skip behavior"
    - path: "functions/src/helpers/listOverflowChain.ts"
      provides: "Shared audit/derivation helpers reused by repair tooling so runtime and repair rules stay aligned"
  key_links:
    - from: "scripts/backfill-list-overflow-metadata.ts"
      to: "functions/src/helpers/listOverflowChain.ts"
      via: "repair tool reuses chain audit and metadata derivation logic from runtime helpers"
      pattern: "getOverflowChainState|buildOverflowListMetadata|buildOverflowListTitle"
---

<objective>
Separate legacy repair work from runtime mutation changes so Phase 06 can land the brownfield backfill safely and independently.

Purpose: Backfill/repair work is necessary for brownfield consistency, but it should not be bundled into the same execution unit as active mutation-path changes.
Output: A dry-run-safe repair script plus focused regression coverage for metadata repair and inconsistency reporting.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@functions/src/helpers/listOverflowChain.ts
@scripts/configure-algolia-list-sorting.mjs

<interfaces>
From `functions/src/helpers/listOverflowChain.ts` after 06-02:
```typescript
export type OverflowChainState = {
  rootListId: string;
  logicalCount: number;
  canMutate: boolean;
  issues: Array<{ code: string; severity: 'warning' | 'blocking'; message: string }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a dry-run-safe list overflow metadata backfill tool</name>
  <files>scripts/backfill-list-overflow-metadata.ts, functions/src/helpers/listOverflowChain.ts</files>
  <behavior>
    - Dry run reports planned root/overflow metadata updates without writing.
    - Apply mode writes missing root ids, overflow depth, logical totals, and canonical continuation names only for consistent chains.
    - Broken or ambiguous chains are reported and skipped with actionable issue output.
  </behavior>
  <action>Create `scripts/backfill-list-overflow-metadata.ts` as a repository repair tool that audits all lists, derives metadata using the shared chain helper, and supports `--dry-run` by default with explicit apply mode for writes. Keep the script non-destructive for inconsistent chains: emit the chain issues and skip writes instead of guessing a repair.</action>
  <verify>
    <automated>pnpm exec ts-node --transpile-only scripts/backfill-list-overflow-metadata.ts --dry-run</automated>
  </verify>
  <done>The repo has a runnable dry-run repair tool for legacy list metadata that uses the same derivation rules as runtime code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused regression coverage for metadata repair and skip behavior</name>
  <files>functions/src/test/lists/backfillListOverflowMetadata.test.ts</files>
  <behavior>
    - Consistent legacy chains produce the expected repair plan and write shape.
    - Dry run produces no writes.
    - Inconsistent chains are surfaced as skips with issue details.
  </behavior>
  <action>Add a focused repair suite that seeds legacy list documents, exercises dry-run and apply-mode behavior through the script/helper boundary, and asserts both successful metadata repair and safe skip behavior for broken chains.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/lists/backfillListOverflowMetadata.test.ts --forceExit"</automated>
  </verify>
  <done>The repair suite passes and locks the dry-run/apply/skip contract for legacy list metadata backfill.</done>
</task>

</tasks>

<verification>
Run the dry-run repair script and the focused backfill emulator suite. Confirm the script reports skipped inconsistent chains instead of applying writes.
</verification>

<success_criteria>
- Legacy list docs can be audited and backfilled by script instead of manual Firestore edits.
- Dry-run is the default safety mode.
- Broken chains are surfaced for manual follow-up instead of being force-repaired.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-06-SUMMARY.md`
</output>
