---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 07
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - packages/contracts/deleteSubsplashList.ts
  - functions/src/deleteSubsplashList.ts
  - functions/src/test/lists/deleteSubsplashListGuard.test.ts
  - apps/web/pages/admin/lists.tsx
  - apps/web/components/ListTable.tsx
autonomous: true
requirements: [OFLOW-05]
must_haves:
  truths:
    - "Deleting a root list with overflow pages is blocked with a clear cascade-aware explanation instead of silently cascading or succeeding."
    - "Single-page lists and roots without overflow still follow the normal delete path."
    - "Admin delete UX surfaces overflow-chain impact clearly and stops without a false success state."
  artifacts:
    - path: "packages/contracts/deleteSubsplashList.ts"
      provides: "Delete contract that can return structured blocked-delete details"
    - path: "functions/src/deleteSubsplashList.ts"
      provides: "Chain-aware delete preflight and blocking response contract"
    - path: "functions/src/test/lists/deleteSubsplashListGuard.test.ts"
      provides: "Emulator coverage for root delete blocking and allowed single-list deletes"
    - path: "apps/web/pages/admin/lists.tsx"
      provides: "Admin list delete flow that surfaces blocked-delete explanations without fake success"
    - path: "apps/web/components/ListTable.tsx"
      provides: "Table-level delete affordance and warning presentation for blocked root deletes"
  key_links:
    - from: "functions/src/deleteSubsplashList.ts"
      to: "functions/src/helpers/listOverflowChain.ts"
      via: "delete preflight uses shared chain audit before allowing remote delete"
      pattern: "getOverflowChainState"
    - from: "apps/web/pages/admin/lists.tsx"
      to: "apps/web/components/ListTable.tsx"
      via: "page surfaces chain-aware blocked-delete details in the admin list table flow"
      pattern: "onDeleteList"
---

<objective>
Separate the destructive-action guard from discovery/search work so delete blocking can be implemented and verified as its own executable unit.

Purpose: Root delete safety is a distinct backend/UI concern and should not be bundled into the same plan as search, sort, and selector cleanup.
Output: A chain-aware delete contract, backend guard, and admin UI that clearly explains blocked root deletes.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@packages/contracts/deleteSubsplashList.ts
@functions/src/deleteSubsplashList.ts
@apps/web/pages/admin/lists.tsx
@apps/web/components/ListTable.tsx

<interfaces>
From `packages/contracts/deleteSubsplashList.ts`:
```typescript
export interface DeleteSubsplashListInputType {
  listId: string;
  operationKey?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chain-aware blocked-delete contract and backend preflight</name>
  <files>packages/contracts/deleteSubsplashList.ts, functions/src/deleteSubsplashList.ts</files>
  <behavior>
    - Root delete is blocked when overflow pages exist and returns machine-readable chain summary details.
    - Single-page or root-without-overflow deletes still work.
    - Backend performs the chain preflight before any remote delete side effect runs.
  </behavior>
  <action>Extend the delete callable contract so it can return a structured blocked-delete payload containing root/overflow summary metadata instead of blindly deleting remote lists. Use `getOverflowChainState` before any remote delete, block root deletes when overflow nodes exist, and keep the existing lock/idempotency behavior for allowed deletes.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>The delete callable can explicitly refuse unsafe root deletes before any remote mutation executes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add delete-guard emulator coverage and blocked-delete admin UX</name>
  <files>functions/src/test/lists/deleteSubsplashListGuard.test.ts, apps/web/pages/admin/lists.tsx, apps/web/components/ListTable.tsx</files>
  <behavior>
    - Emulator tests prove root delete blocking, allowed non-overflow deletes, and stable blocked-delete payloads.
    - Admin delete flow explains that overflow pages exist and stops without showing a success path.
    - Delete confirmation/warning copy includes cascade-aware chain impact.
  </behavior>
  <action>Add a focused delete-guard emulator suite, then update the admin lists page/table flow to catch the blocked-delete response and show a clear overflow explanation. Keep the UI sequential with 06-03 by reusing the same table/page files rather than introducing a second delete surface.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/lists/deleteSubsplashListGuard.test.ts --forceExit" && pnpm --dir apps/web exec eslint pages/admin/lists.tsx components/ListTable.tsx</automated>
  </verify>
  <done>Unsafe root deletes are blocked with a clear explanation in both backend tests and admin UI behavior.</done>
</task>

</tasks>

<verification>
Run the delete-guard emulator suite and frontend lint checks, then confirm blocked root deletes stop cleanly without a generic success path.
</verification>

<success_criteria>
- Root delete attempts stop with a chain-aware explanation instead of silently cascading or succeeding.
- Allowed non-overflow deletes still work.
- Delete safety is isolated in its own plan rather than mixed into discovery/search work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-07-SUMMARY.md`
</output>
