---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-04"]
files_modified:
  - packages/contracts/reorderListItems.ts
  - functions/src/reorderListItems.ts
  - functions/src/test/lists/reorderListItems.test.ts
  - apps/web/pages/admin/lists/[listId].tsx
  - apps/web/pages/admin/lists/[listId].test.tsx
autonomous: true
requirements: [OFLOW-04, OFLOW-06]
must_haves:
  truths:
    - "Reordering from the root detail page applies to the whole logical list, not just the first physical Subsplash page."
    - "The backend remaps logical order back into overflow-page boundaries and preserves continuation link rows correctly."
    - "Reorder is blocked when chain audit detects inconsistencies, so unsafe writes do not run against broken chains."
  artifacts:
    - path: "packages/contracts/reorderListItems.ts"
      provides: "Root-aware reorder contract that distinguishes logical root ordering from physical page ids"
    - path: "functions/src/reorderListItems.ts"
      provides: "Chain-aware reorder callable that repartitions logical order across overflow pages under lock/idempotency"
    - path: "functions/src/test/lists/reorderListItems.test.ts"
      provides: "Regression coverage for overflow repartitioning and blocked inconsistent-chain reorder"
    - path: "apps/web/pages/admin/lists/[listId].tsx"
      provides: "Root detail save flow that submits logical order and respects mutation blocking flags"
    - path: "apps/web/pages/admin/lists/[listId].test.tsx"
      provides: "Page-level regression coverage for disabled save-order behavior when chain state blocks mutation"
  key_links:
    - from: "functions/src/reorderListItems.ts"
      to: "functions/src/helpers/listOverflowChain.ts"
      via: "reorder uses shared chain audit before repartitioning rows"
      pattern: "getOverflowChainState"
    - from: "functions/src/reorderListItems.ts"
      to: "packages/contracts/reorderListItems.ts"
      via: "callable input/output shape matches root-aware reorder contract"
      pattern: "rootListId|logicalItemOrder"
    - from: "apps/web/pages/admin/lists/[listId].tsx"
      to: "functions/src/reorderListItems.ts"
      via: "save-order action sends one logical ordering payload for the root chain"
      pattern: "createReorderListItems"
---

<objective>
Upgrade reorder from a single physical-list patch into a root-aware logical-chain remap that preserves overflow boundaries safely.

Purpose: The root detail page should let admins reorder one logical list while the backend handles Subsplash pagination/link-row constraints and refuses to mutate inconsistent chains.
Output: Root-aware reorder contract, chain repartition logic in the callable, updated root-detail save flow, and regression tests.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@functions/src/reorderListItems.ts
@functions/src/helpers/listOverflowChain.ts
@apps/web/pages/admin/lists/[listId].tsx

<interfaces>
From `packages/contracts/reorderListItems.ts`:
```typescript
export interface ReorderListItemsInputType {
  firestoreListId: string;
  itemOrder: ListItemOrderEntry[];
  operationKey?: string;
}
```

From `functions/src/reorderListItems.ts`:
```typescript
export interface ReorderListItemsOutputType {
  status: 'success' | 'error';
  message: string;
  firestoreListId: string;
  subsplashListId?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace single-page reorder semantics with a root-aware logical-chain contract</name>
  <files>packages/contracts/reorderListItems.ts, functions/src/reorderListItems.ts</files>
  <behavior>
    - Reorder input targets a root list/logical chain, not one physical Subsplash page.
    - Callable blocks when chain audit reports blocking issues.
    - Callable repartitions the logical order across physical pages using non-terminal capacity rules and preserves continuation link rows.
  </behavior>
  <action>Expand the reorder contract so the caller submits one logical item ordering for the root list chain. Refactor the callable to resolve chain state first, acquire the necessary list locks for the whole chain, partition logical media rows back into physical pages using the existing overflow link-row rule (`maxListSize - 1` content rows for non-terminal pages), patch every remote list in order, and keep Firestore positions aligned with the logical order only after remote success.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Reorder no longer assumes a single remote list and can safely remap one logical order across the overflow chain.</done>
</task>

<task type="auto">
  <name>Task 2: Wire root-detail save flow to the logical reorder contract and honor mutation blocking</name>
  <files>apps/web/pages/admin/lists/[listId].tsx, apps/web/pages/admin/lists/[listId].test.tsx, packages/contracts/reorderListItems.ts</files>
  <action>Update the root detail page to submit one logical order payload for the root chain, not only the currently synced rows on the first page. Respect the `canMutate`/issue state introduced in 06-04 by disabling save-order when the chain is inconsistent or the local mirror coverage warning is present, and keep the UI messaging explicit about why reorder is unavailable. Preserve the existing local Firestore position update flow only after the root-aware reorder callable succeeds, and add a focused page-level regression test that proves the save action stays disabled and the callable is not invoked while mutation blocking is active.</action>
  <verify>
    <automated>pnpm --dir apps/web exec eslint pages/admin/lists/[listId].tsx</automated>
    <automated>pnpm --dir apps/web exec jest --config jest.config.js pages/admin/lists/[listId].test.tsx --runInBand</automated>
  </verify>
  <done>The root detail page sends logical reorder payloads and never attempts reorder when chain health forbids mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend reorder regression tests for overflow repartitioning and blocked chains</name>
  <files>functions/src/test/lists/reorderListItems.test.ts</files>
  <behavior>
    - Reordering a multi-page chain reshapes remote rows across root and overflow pages while preserving link rows.
    - Missing/duplicate/broken chain metadata blocks reorder before any remote patch runs.
    - Successful reorder keeps response payloads stable for the updated root-aware contract.
  </behavior>
  <action>Extend the existing reorder test suite with multi-page overflow fixtures, including at least one case where items cross a page boundary after reorder and one blocking inconsistency case that proves no remote write occurs. Reuse the existing Subsplash mock infrastructure so the tests assert final row layout per page, not just top-level success.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/lists/reorderListItems.test.ts --forceExit"</automated>
  </verify>
  <done>The reorder suite proves logical-chain remapping works and inconsistent chains are blocked before side effects.</done>
</task>

</tasks>

<verification>
Run the reorder emulator suite, the focused root-detail page test, and confirm the detail page no longer exposes an enabled save action when chain issues are blocking mutations.
</verification>

<success_criteria>
- Root-detail reorder applies to the whole logical list chain, not just the first physical page.
- Remote Subsplash pages are repartitioned correctly with continuation rows preserved.
- Inconsistent chains block reorder before any remote patch executes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-05-SUMMARY.md`
</output>
