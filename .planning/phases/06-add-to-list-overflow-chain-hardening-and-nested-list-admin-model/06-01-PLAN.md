---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/types/List.ts
  - apps/web/types/List.ts
  - packages/contracts/getListOverflowChain.ts
  - functions/src/helpers/listOverflowChain.ts
  - functions/src/getListOverflowChain.ts
  - functions/src/index.ts
  - functions/src/test/lists/getListOverflowChain.test.ts
autonomous: true
requirements: [OFLOW-03, OFLOW-04]
must_haves:
  truths:
    - "Any list document can be resolved to one logical root chain, even when the caller starts from an overflow page id."
    - "Backend chain reads expose ordered overflow-page metadata, logical totals, and integrity issues in one normalized response."
    - "Blocking chain inconsistencies are explicit so admin surfaces can disable risky actions instead of guessing."
  artifacts:
    - path: "packages/shared/types/List.ts"
      provides: "Canonical list metadata fields for root/overflow identity and logical totals"
    - path: "packages/contracts/getListOverflowChain.ts"
      provides: "Typed callable contract for normalized chain reads"
      exports: ["GetListOverflowChainInputType", "GetListOverflowChainOutputType"]
    - path: "functions/src/helpers/listOverflowChain.ts"
      provides: "Shared chain traversal, legacy fallback inference, and audit issue generation"
    - path: "functions/src/getListOverflowChain.ts"
      provides: "Admin-authenticated callable that returns normalized root-chain state"
      exports: ["default"]
    - path: "functions/src/test/lists/getListOverflowChain.test.ts"
      provides: "Emulator coverage for root resolution, logical totals, and blocking inconsistency detection"
  key_links:
    - from: "functions/src/getListOverflowChain.ts"
      to: "functions/src/helpers/listOverflowChain.ts"
      via: "callable delegates normalized chain resolution to shared helper"
      pattern: "getOverflowChainState"
    - from: "functions/src/index.ts"
      to: "functions/src/getListOverflowChain.ts"
      via: "lower-case export for v2 callable routing"
      pattern: "exports\\.getlistoverflowchain\\s*=\\s*getlistoverflowchain"
    - from: "packages/shared/types/List.ts"
      to: "apps/web/types/List.ts"
      via: "frontend/backend list contracts stay in lockstep on root/overflow metadata"
      pattern: "isRootList|rootListId|overflowDepth|logicalCount|hasOverflowPages"
---

<objective>
Establish the explicit logical-root list contract and a server-owned chain-audit read model that every admin surface can trust.

Purpose: Phase 06 cannot safely hide overflow pages, aggregate totals, or block risky actions until the system has one authoritative definition of root-vs-overflow identity and chain health.
Output: Shared list metadata fields, a typed `getlistoverflowchain` callable, a reusable chain helper, and emulator regression coverage.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@packages/shared/types/List.ts
@apps/web/types/List.ts
@functions/src/addToList.ts
@functions/src/firestoreDataConverter.ts
@functions/src/index.ts

<interfaces>
From `packages/shared/types/List.ts`:
```typescript
export interface List {
  id: string;
  name: string;
  count?: number;
  subsplashId?: string;
  moreSermonsRef?: string;
  isMoreSermonsList?: boolean;
}
```

From `functions/src/index.ts`:
```typescript
exports.deletesubsplashlist = deletesubsplashlist;
exports.reorderlistitems = reorderlistitems;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define explicit root/overflow metadata and chain-state contracts</name>
  <files>packages/shared/types/List.ts, apps/web/types/List.ts, packages/contracts/getListOverflowChain.ts</files>
  <behavior>
    - Root lists carry self-referential logical-root metadata (`isRootList`, `rootListId`, `overflowDepth=0`).
    - Overflow pages carry the same logical-root id plus non-zero depth metadata.
    - Chain-read contracts expose normalized nodes, logical totals, redirect target id, and blocking/warning issues without `any`.
  </behavior>
  <action>Extend both shared and app-side `List` interfaces with explicit root/overflow metadata fields and a root-only logical total field while preserving legacy `isMoreSermonsList`/`moreSermonsRef` compatibility for brownfield documents. Add a new contracts file for `getlistoverflowchain` that describes ordered nodes, logical counts, and machine-readable issues the UI can consume directly.</action>
  <verify>
    <automated>pnpm run build:packages && pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>List metadata and chain-read contracts compile cleanly across shared packages, frontend imports, and functions code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared chain traversal/audit helper and callable export</name>
  <files>functions/src/helpers/listOverflowChain.ts, functions/src/getListOverflowChain.ts, functions/src/index.ts</files>
  <behavior>
    - Helper resolves the root chain even when input starts from an overflow list id.
    - Helper computes ordered nodes, logical totals, redirect target root id, and blocking issues for cycles/missing links/name drift.
    - Callable rejects unauthorized callers and returns one normalized response shape for admin surfaces.
  </behavior>
  <action>Create `functions/src/helpers/listOverflowChain.ts` with server-owned traversal/audit logic that can use explicit metadata first and legacy inference as a fallback so Phase 06 can migrate safely. Add `functions/src/getListOverflowChain.ts` as an admin/publisher-authenticated callable, export it from `functions/src/index.ts` with the lower-case key `getlistoverflowchain`, and keep issue codes stable because later plans will gate delete/reorder/UI warnings on them.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Admin surfaces can call one exported endpoint to resolve any list into an auditable logical root-chain state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add emulator coverage for root resolution and inconsistency detection</name>
  <files>functions/src/test/lists/getListOverflowChain.test.ts</files>
  <behavior>
    - Root input returns itself as `rootListId` with ordered chain nodes and logical total.
    - Overflow input resolves to the root list and reports the correct redirect target.
    - Missing link targets, duplicate depth, or broken parent-child continuity produce blocking issues and `canMutate=false`.
  </behavior>
  <action>Add focused emulator tests that seed root + overflow Firestore docs in both explicit-metadata and legacy-inference shapes, then assert normalized callable/helper output. Include at least one broken-chain case so later UI plans can safely disable reorder/delete when `canMutate` is false.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/lists/getListOverflowChain.test.ts --forceExit"</automated>
  </verify>
  <done>The chain-read suite passes and locks root resolution, logical totals, and blocking inconsistency semantics.</done>
</task>

</tasks>

<verification>
Run the new chain-read emulator suite and confirm `getlistoverflowchain` is exported from the functions index with no TypeScript drift across shared/app list contracts.
</verification>

<success_criteria>
- The repo has one explicit root/overflow list contract instead of relying only on implicit `isMoreSermonsList` checks.
- A typed callable can resolve any list id into normalized root-chain state with logical totals and issue codes.
- Emulator tests cover root input, overflow input, and broken-chain blocking output.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-01-SUMMARY.md`
</output>
