---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - apps/web/utils/lists/listOverflowChainView.ts
  - apps/web/utils/lists/listOverflowChainView.test.ts
  - apps/web/components/admin/lists/OverflowChainPanel.tsx
  - apps/web/components/admin/lists/ListBoundaryMarker.tsx
  - apps/web/pages/admin/lists/[listId].tsx
  - apps/web/pages/admin/lists/[listId].test.tsx
autonomous: true
requirements: [OFLOW-03, OFLOW-04]
must_haves:
  truths:
    - "Direct navigation to an overflow list route redirects to the root list detail page."
    - "The root list detail page shows one aggregated logical sermon list across the whole overflow chain, but only for sermons that have local mirrored `listItems` rows."
    - "Admins can see subtle page-boundary markers plus an inspect-only chain panel with names, ids, counts, and depth/order metadata."
    - "When the chain is inconsistent or local mirror coverage is incomplete, the page stays readable but shows a clear warning and disables risky actions."
  artifacts:
    - path: "apps/web/utils/lists/listOverflowChainView.ts"
      provides: "Frontend helper that shapes callable chain state into page sections, markers, and UI flags"
    - path: "apps/web/components/admin/lists/OverflowChainPanel.tsx"
      provides: "Inspect-only chain metadata panel for root detail"
    - path: "apps/web/components/admin/lists/ListBoundaryMarker.tsx"
      provides: "Subtle page-boundary markers inside the aggregated sermon list"
    - path: "apps/web/pages/admin/lists/[listId].tsx"
      provides: "Root-only detail page with redirect, locally mirrored aggregated items, diagnostics, and action gating"
    - path: "apps/web/pages/admin/lists/[listId].test.tsx"
      provides: "Page-level regression coverage for root redirects, warning banners, and disabled risky controls"
  key_links:
    - from: "apps/web/pages/admin/lists/[listId].tsx"
      to: "packages/contracts/getListOverflowChain.ts"
      via: "page resolves root/overflow state before rendering or redirecting"
      pattern: "getlistoverflowchain"
    - from: "apps/web/pages/admin/lists/[listId].tsx"
      to: "apps/web/components/admin/lists/OverflowChainPanel.tsx"
      via: "root detail renders inspect-only chain metadata"
      pattern: "OverflowChainPanel"
    - from: "apps/web/pages/admin/lists/[listId].tsx"
      to: "apps/web/components/admin/lists/ListBoundaryMarker.tsx"
      via: "aggregated list view inserts page-boundary markers without breaking unified ordering"
      pattern: "ListBoundaryMarker"
---

<objective>
Turn `/admin/lists/[listId]` into the single logical-root management surface with redirect, aggregation, diagnostics, and safe read-only degradation.

Purpose: Admins should never manage continuation pages directly. The detail page needs to present one logical list view while still revealing enough chain structure to debug overflow behavior.
Output: Root-only detail routing, locally mirrored aggregated list rendering, boundary markers, chain panel, and inconsistency warnings/action gating.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@apps/web/pages/admin/lists/[listId].tsx
@packages/contracts/getListOverflowChain.ts
@functions/src/getListOverflowChain.ts

<interfaces>
From `packages/contracts/getListOverflowChain.ts` after 06-01:
```typescript
export interface GetListOverflowChainOutputType {
  rootListId: string;
  redirectToRootListId?: string;
  logicalCount: number;
  canMutate: boolean;
  nodes: Array<{ firestoreListId: string; name: string; subsplashId?: string; depth: number; physicalCount: number }>;
  issues: Array<{ code: string; severity: 'warning' | 'blocking'; message: string }>;
}
```

From `apps/web/pages/admin/lists/[listId].tsx`:
```typescript
type ListDetailItem = Sermon & {
  position?: number;
  uploadStatus?: listUploadStatus;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add logical-chain view helpers and root redirect flow</name>
  <files>apps/web/utils/lists/listOverflowChainView.ts, apps/web/utils/lists/listOverflowChainView.test.ts, apps/web/pages/admin/lists/[listId].tsx</files>
  <behavior>
    - Visiting an overflow list id redirects to the root list id before mutating controls render.
    - Aggregated detail loading combines root + overflow list items into one logical order for display using only locally mirrored Firestore `listItems`.
    - Helper output includes boundary marker placement and action gating derived from `canMutate`/issues.
  </behavior>
  <action>Create a frontend helper that transforms `getlistoverflowchain` output plus per-list Firestore item snapshots into one logical list view model. Update the detail page to call the chain endpoint first, redirect overflow ids to the root detail route, and aggregate locally mirrored `listItems` from every node in the chain into a single ordered display without exposing per-page management surfaces. Do not fabricate remote-only sermons that exist only in Subsplash page counts; instead, compare each node's physical count from the chain response with the mirrored Firestore rows and surface any gap as chain diagnostics/read-only warnings.</action>
  <verify>
    <automated>pnpm --dir apps/web exec jest --config jest.config.js utils/lists/listOverflowChainView.test.ts --runInBand</automated>
    <automated>pnpm --dir apps/web exec jest --config jest.config.js pages/admin/lists/[listId].test.tsx --runInBand</automated>
  </verify>
  <done>The detail page resolves overflow ids to the root route and can build a single logical view model for the whole chain.</done>
</task>

<task type="auto">
  <name>Task 2: Render chain diagnostics, page-boundary markers, and safe read-only warnings on the root detail page</name>
  <files>apps/web/components/admin/lists/OverflowChainPanel.tsx, apps/web/components/admin/lists/ListBoundaryMarker.tsx, apps/web/pages/admin/lists/[listId].tsx, apps/web/pages/admin/lists/[listId].test.tsx</files>
  <action>Add an inspect-only chain panel that shows overflow-page name, Subsplash id, physical item count, mirrored local row count, and depth/order, with lightweight helpers like copy/open-link affordances but no page-specific mutation actions. Insert subtle boundary markers into the unified sermon list where each Subsplash page boundary falls. When chain issues exist or the local mirror coverage is incomplete, keep the page readable, show a clear warning banner, and disable risky controls like save-order (and any future destructive actions on this page) until the chain is consistent. Cover redirect, warning-banner rendering, and disabled-action behavior with a focused page-level test instead of relying only on manual verification.</action>
  <verify>
    <automated>pnpm --dir apps/web exec eslint pages/admin/lists/[listId].tsx components/admin/lists/OverflowChainPanel.tsx components/admin/lists/ListBoundaryMarker.tsx utils/lists/listOverflowChainView.ts</automated>
    <automated>pnpm --dir apps/web exec jest --config jest.config.js pages/admin/lists/[listId].test.tsx --runInBand</automated>
  </verify>
  <done>The root detail page is the only admin entry point, shows chain diagnostics/boundaries, and degrades to readable-but-safe mode when inconsistencies are detected.</done>
</task>

</tasks>

<verification>
Run the new chain-view helper tests, the focused root-detail page tests, and lint the detail-page surface, then manually confirm that an overflow route redirects to the root route without showing editable overflow-page controls.
</verification>

<success_criteria>
- Overflow list URLs resolve to the root detail page.
- Root detail renders one aggregated sermon list with page-boundary context and a compact inspect-only chain panel.
- Inconsistent chains remain readable but clearly warn admins and disable risky actions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-04-SUMMARY.md`
</output>
