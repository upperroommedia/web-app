---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - apps/web/utils/algolia/searchRecords.ts
  - apps/web/utils/algolia/searchRecords.test.ts
  - apps/web/utils/algolia/listSorting.ts
  - apps/web/utils/algolia/listSorting.test.ts
  - apps/web/utils/mockAlgoliaSearchClient.ts
  - apps/web/components/ListSelector.tsx
  - apps/web/components/uploaderComponents/SpeakerSelector.tsx
  - apps/web/components/uploaderComponents/UploaderComponent.tsx
  - apps/web/components/ListTable.tsx
  - apps/web/pages/admin/lists.tsx
autonomous: true
requirements: [OFLOW-01, OFLOW-02]
must_haves:
  truths:
    - "Overflow pages never appear as selectable/discoverable list entities in uploader or admin discovery flows."
    - "Admin list discovery shows logical chain totals on root rows and can surface an admin-only overflow indicator."
    - "Brownfield discovery remains migration-safe by honoring explicit root metadata when present while still excluding legacy overflow pages before full rollout verification."
    - "Speaker-driven automatic list assignment follows the same root-only contract and never adds an overflow speaker list to the sermon."
    - "Admin list discovery does not expose misleading `count`-based sorting while logical-total sort/index rollout is still deferred."
    - "This phase preserves query-time filtering but does not require new Algolia replica/index settings or production-only indexed fields beyond what the current indexing path already guarantees."
  artifacts:
    - path: "apps/web/utils/algolia/searchRecords.ts"
      provides: "Canonical root-only Algolia filter and logical-total normalization"
    - path: "apps/web/utils/algolia/listSorting.ts"
      provides: "Discovery sort options constrained to replicas that remain truthful before logical-total replica rollout exists"
    - path: "apps/web/components/ListSelector.tsx"
      provides: "Root-only selector behavior for uploader/admin assignment flows"
    - path: "apps/web/components/uploaderComponents/SpeakerSelector.tsx"
      provides: "Speaker-linked list auto-assignment that excludes overflow speaker lists under the same root-only fallback contract"
    - path: "apps/web/components/ListTable.tsx"
      provides: "Admin list table rows with logical totals, overflow indicators, and name-only sorting while count-sort is suppressed"
  key_links:
    - from: "apps/web/utils/algolia/searchRecords.ts"
      to: "apps/web/utils/mockAlgoliaSearchClient.ts"
      via: "real and mock search clients must share the same migration-safe root discovery contract"
      pattern: "isRootList|isMoreSermonsList|logicalCount|count"
    - from: "apps/web/pages/admin/lists.tsx"
      to: "apps/web/components/ListTable.tsx"
      via: "discovery page passes logical totals and overflow indicator state into table rows"
      pattern: "logicalCount|count|hasOverflowPages|moreSermonsRef"
    - from: "apps/web/pages/admin/lists.tsx"
      to: "apps/web/utils/algolia/listSorting.ts"
      via: "admin discovery must suppress sort modes that still depend on physical `count` replicas"
      pattern: "count|name|resolveListIndexName|getDefaultListSortOrder"
    - from: "apps/web/components/uploaderComponents/SpeakerSelector.tsx"
      to: "apps/web/components/ListSelector.tsx"
      via: "speaker-linked list assignment must use the same root-only fallback rules as manual list selection"
      pattern: "isRootList|isMoreSermonsList|moreSermonsRef|hasOverflowPages"
---

<objective>
Make list discovery and selection root-only everywhere so admins and uploaders only interact with logical root lists, without breaking brownfield data before metadata rollout is verified.

Purpose: Admin operators and uploaders should interact with one logical list entity, not a collection of hidden continuation pages, but Phase 06 cannot require strict `isRootList:true` / `logicalCount` enforcement until the brownfield metadata rollout is proven.
Output: Migration-safe root-only search/selector/table behavior, speaker-linked list assignment parity, and logical total/overflow indicators with legacy fallback rules.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@apps/web/components/ListSelector.tsx
@apps/web/components/uploaderComponents/SpeakerSelector.tsx
@apps/web/components/ListTable.tsx
@apps/web/pages/admin/lists.tsx
@apps/web/utils/algolia/searchRecords.ts
@apps/web/utils/mockAlgoliaSearchClient.ts

<interfaces>
From `apps/web/utils/algolia/searchRecords.ts`:
```typescript
export interface AlgoliaListHit extends Partial<List> {
  objectID: string;
}
export const normalizeAlgoliaListHit = (hit: AlgoliaListHit): List => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert discovery/search surfaces to a migration-safe root-only model</name>
  <files>apps/web/utils/algolia/searchRecords.ts, apps/web/utils/algolia/searchRecords.test.ts, apps/web/utils/mockAlgoliaSearchClient.ts</files>
  <behavior>
    - Discovery prefers explicit root metadata when present but still excludes legacy overflow pages before rollout is verified.
    - List result normalization displays `logicalCount` when present and falls back to `count` for legacy hits.
    - Mock Algolia behavior matches the same migration-safe discovery semantics in emulator mode.
  </behavior>
  <action>Update the list search utilities so brownfield discovery does not hard-require `isRootList:true` yet. Prefer explicit root metadata when it exists, but preserve migration-safe legacy overflow exclusion until the metadata backfill rollout is complete and verified. Keep the root-only distinction enforced at query time for Algolia and Firestore-backed fallback queries instead of post-fetch UI filtering. Normalize hits so discovery surfaces render `logicalCount ?? count`, and add focused unit coverage for the fallback behavior so strict enforcement does not slip in early.</action>
  <verify>
    <automated>pnpm --dir apps/web exec jest --config jest.config.js utils/algolia/searchRecords.test.ts --runInBand</automated>
  </verify>
  <done>Real and mock list discovery use the same migration-safe root-only contract and surface logical totals without requiring completed backfill rollout.</done>
</task>

<task type="auto">
  <name>Task 2: Hide overflow pages from selectors/tables and surface root overflow indicators</name>
  <files>apps/web/utils/algolia/listSorting.ts, apps/web/utils/algolia/listSorting.test.ts, apps/web/components/ListSelector.tsx, apps/web/components/uploaderComponents/SpeakerSelector.tsx, apps/web/components/uploaderComponents/UploaderComponent.tsx, apps/web/components/ListTable.tsx, apps/web/pages/admin/lists.tsx</files>
  <action>Remove the scattered ad hoc overflow filtering pattern and centralize a migration-safe root-only check across manual list selection, speaker-driven automatic list assignment, uploader fallbacks, and the admin list table: explicit root metadata wins when present, otherwise legacy `isMoreSermonsList !== true` behavior continues until rollout verification is complete. Update `SpeakerSelector.tsx` so auto-added speaker lists use that same contract and never attach overflow continuation pages. Add an admin-only overflow indicator/badge on root rows using `hasOverflowPages` when present and safe legacy fallbacks (`moreSermonsRef`) otherwise, render logical totals as `logicalCount ?? count`, and change `listSorting.ts` plus the admin list page so this phase only exposes sort modes backed by truthful existing replicas. Do not introduce or require new Algolia replica settings, index configuration scripts, or production indexing changes for `logicalCount`/`hasOverflowPages` in this phase; defer those to a later rollout-specific phase.</action>
  <verify>
    <automated>pnpm --dir apps/web exec eslint components/ListSelector.tsx components/uploaderComponents/SpeakerSelector.tsx components/uploaderComponents/UploaderComponent.tsx components/ListTable.tsx pages/admin/lists.tsx</automated>
    <automated>pnpm --dir apps/web exec jest --config jest.config.js utils/algolia/listSorting.test.ts --runInBand</automated>
  </verify>
  <done>Uploader/admin selection surfaces and speaker-linked list assignment only use logical root lists, and the admin list table shows logical totals plus overflow awareness without exposing inconsistent count-based sort behavior.</done>
</task>

</tasks>

<verification>
Run the list-search unit tests and frontend lint checks, then confirm list discovery uses logical totals and migration-safe root-only filtering in both Algolia and emulator modes.
</verification>

<success_criteria>
- Overflow pages are hidden from uploader/admin discovery and selection flows.
- Speaker-linked automatic list assignment never adds an overflow list.
- Admin list discovery shows logical totals on root rows and can signal overflow presence.
- Discovery remains safe for brownfield data until the metadata backfill rollout is explicitly verified.
- Admin list discovery does not offer misleading count-based sorting before logical-total sort rollout is ready.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-03-SUMMARY.md`
</output>
