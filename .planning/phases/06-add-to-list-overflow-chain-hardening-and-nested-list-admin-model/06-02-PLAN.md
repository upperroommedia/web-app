---
phase: 06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - functions/src/helpers/listOverflowChain.ts
  - functions/src/addToList.ts
  - functions/src/removeFromList.ts
  - functions/src/editSubsplashList.ts
  - functions/src/Scrapers/populateListsHelper.ts
  - functions/src/test/addToList/basic.test.ts
  - functions/src/test/addToList/pageNumberIncrementation.test.ts
  - functions/src/test/removeFromList/basic.test.ts
  - functions/src/test/lists/editSubsplashListOverflowNaming.test.ts
autonomous: true
requirements: [OFLOW-02, OFLOW-06]
must_haves:
  truths:
    - "New and imported overflow pages persist explicit logical-root metadata instead of relying on ad hoc inference."
    - "Root lists keep canonical overflow-page names and root-level logical totals/overflow flags aligned after add, remove, edit, and import flows."
  artifacts:
    - path: "functions/src/addToList.ts"
      provides: "Overflow creation path that writes root/overflow metadata and canonical continuation names"
    - path: "functions/src/editSubsplashList.ts"
      provides: "Root edit path that keeps overflow-page naming aligned to the canonical pattern"
    - path: "functions/src/Scrapers/populateListsHelper.ts"
      provides: "Import path that seeds explicit root metadata for known lists"
    - path: "functions/src/test/lists/editSubsplashListOverflowNaming.test.ts"
      provides: "Regression coverage for canonical overflow-name cascades during root metadata edits"
  key_links:
    - from: "functions/src/addToList.ts"
      to: "functions/src/helpers/listOverflowChain.ts"
      via: "overflow creation uses shared metadata/naming helpers"
      pattern: "buildOverflowListMetadata|buildOverflowListTitle"
    - from: "functions/src/editSubsplashList.ts"
      to: "functions/src/helpers/listOverflowChain.ts"
      via: "root rename cascades canonical overflow titles through chain traversal"
      pattern: "syncOverflowChainNames"
---

<objective>
Persist the Phase 06 data model on real list write/import paths so root-only admin behavior can rely on stored metadata instead of best-effort inference.

Purpose: The new read model is not enough by itself; Phase 06 also needs the list lifecycle to keep explicit chain metadata, logical totals, overflow flags, and canonical naming up to date over time.
Output: Metadata-aware add/remove/edit/import flows and regression tests for overflow naming + logical-root fields.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yasaad/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-CONTEXT.md
@.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-RESEARCH.md
@functions/src/helpers/listOverflowChain.ts
@functions/src/addToList.ts
@functions/src/removeFromList.ts
@functions/src/editSubsplashList.ts
@functions/src/Scrapers/populateListsHelper.ts

<interfaces>
From `packages/shared/types/List.ts` after 06-01:
```typescript
export interface List {
  isRootList?: boolean;
  rootListId?: string;
  overflowDepth?: number;
  logicalCount?: number;
  hasOverflowPages?: boolean;
}
```

From `functions/src/helpers/listOverflowChain.ts` after 06-01:
```typescript
export type OverflowChainState = {
  rootListId: string;
  logicalCount: number;
  canMutate: boolean;
  nodes: Array<{ firestoreListId: string; subsplashId?: string; depth: number; physicalCount: number }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update add/remove/import flows to persist explicit chain metadata and logical totals</name>
  <files>functions/src/helpers/listOverflowChain.ts, functions/src/addToList.ts, functions/src/removeFromList.ts, functions/src/Scrapers/populateListsHelper.ts</files>
  <behavior>
    - New overflow pages persist `isRootList=false`, `rootListId`, non-zero `overflowDepth`, and canonical continuation names.
    - Root docs persist `isRootList=true`, self `rootListId`, `logicalCount`, and `hasOverflowPages` after add/remove mutations.
    - Imported top-level lists seed the explicit root metadata instead of leaving root identity implicit.
  </behavior>
  <action>Refactor list mutation/import paths to reuse shared metadata builders from `functions/src/helpers/listOverflowChain.ts` instead of inlining title/depth logic. Update `addToList` and `removeFromList` to maintain root-level logical totals/overflow flags whenever the chain changes, and update `populateListsHelper` so brownfield imported lists start as explicit roots. Keep lock/idempotency behavior intact and do not introduce new remote side effects inside Firestore transactions beyond the existing pattern.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Root/overflow documents persist explicit metadata and logical totals across add, remove, and import flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical overflow rename cascade for root metadata edits</name>
  <files>functions/src/editSubsplashList.ts, functions/src/helpers/listOverflowChain.ts</files>
  <behavior>
    - Renaming a root list keeps overflow pages aligned to `More {root list name} sermons`.
    - Root-only edits never rename overflow pages into nested `More More ...` variants.
    - Existing lock/idempotency wrappers stay intact around remote title updates.
  </behavior>
  <action>Extend `editSubsplashList` so root metadata edits cascade canonical titles through the overflow chain while preserving existing lock/idempotency wrappers. Reuse the shared chain helper for ordered traversal and canonical name generation so runtime list edits and later repair tooling derive the same overflow naming rules.</action>
  <verify>
    <automated>pnpm --dir functions exec tsc --noEmit</automated>
  </verify>
  <done>Root rename behavior keeps overflow titles canonical without introducing nested continuation-name drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regression coverage for metadata persistence and canonical naming</name>
  <files>functions/src/test/addToList/basic.test.ts, functions/src/test/addToList/pageNumberIncrementation.test.ts, functions/src/test/removeFromList/basic.test.ts, functions/src/test/lists/editSubsplashListOverflowNaming.test.ts</files>
  <behavior>
    - Overflow creation tests assert explicit metadata fields and canonical `More {root} sermons` naming.
    - Remove tests assert root logical totals and overflow flags update when overflow pages shrink or disappear.
    - Edit tests assert root rename cascades canonical overflow names across the full chain.
  </behavior>
  <action>Extend the existing add/remove suites instead of duplicating fixtures, then add a focused edit-name cascade suite for root rename behavior. Keep the assertions at the Firestore document level so future UI work can trust the stored metadata contract.</action>
  <verify>
    <automated>pnpm --dir functions exec firebase emulators:exec --only auth,firestore,database --config ../firebase.test.json "pnpm exec jest --runInBand src/test/addToList/basic.test.ts src/test/addToList/pageNumberIncrementation.test.ts src/test/removeFromList/basic.test.ts src/test/lists/editSubsplashListOverflowNaming.test.ts --forceExit"</automated>
  </verify>
  <done>The mutation/edit suites pass and lock the explicit metadata, logical totals, and canonical naming rules.</done>
</task>

</tasks>

<verification>
Run the focused add/remove/edit emulator suites and confirm root metadata edits preserve canonical overflow names while list mutations keep explicit chain metadata aligned.
</verification>

<success_criteria>
- Root/overflow metadata is stored explicitly on new and imported list docs.
- Root logical totals and overflow flags stay aligned as list membership changes.
- Root renames keep overflow-page titles canonical during normal edit flows.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-to-list-overflow-chain-hardening-and-nested-list-admin-model/06-02-SUMMARY.md`
</output>
