---
phase: 01-series-subtitle-automation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - components/ManagePublishingPopup.tsx
  - components/UploadStatusList.tsx
  - pages/admin/series/[seriesId].tsx
  - functions/src/helpers/seriesHelpers.ts
  - functions/src/DocumentListeners/Series/seriesItemOnWrite.ts
  - functions/src/addToSeries.ts
  - functions/src/index.ts
  - functions/src/test/series/seriesMetadata.test.ts
  - functions/src/test/series/addToSeries.test.ts
  - functions/src/test/series/*
  - scripts/backfillSeriesPublishedFlags.ts
  - docs/series-publish-workflow.md
autonomous: true
requirements:
  - adhoc-series-publish-independent-from-lists
  - adhoc-publish-everywhere-shortcut
user_setup: []
must_haves:
  truths:
    - "Publishing a sermon to a Subsplash series does not require publishing to any Subsplash lists."
    - "Series published count increments only when `seriesItems/{sermonId}.publishedToSubsplash === true`."
    - "No fallback inference from `sermonSubsplashId` is used for series published state."
    - "Users can still publish to lists and series together via a single convenience action."
  artifacts:
    - path: "components/ManagePublishingPopup.tsx"
      provides: "Independent series publish action plus optional combined publish action."
      min_lines: 40
    - path: "functions/src/helpers/seriesHelpers.ts"
      provides: "Strict published-count derivation from explicit `publishedToSubsplash` only."
      min_lines: 20
    - path: "scripts/backfillSeriesPublishedFlags.ts"
      provides: "Admin backfill to reconcile legacy series item publish flags from real Subsplash membership."
      min_lines: 50
    - path: "docs/series-publish-workflow.md"
      provides: "Operator workflow for independent vs combined publishing and backfill usage."
  key_links:
    - from: "ManagePublishingPopup publish-to-series action"
      to: "functions/src/addToSeries.ts"
      via: "addtoseries callable then Firestore seriesItem publish flag update"
    - from: "series/{seriesId}/seriesItems/{itemId} writes"
      to: "series/{seriesId}.publishedItemCount + subtitle"
      via: "seriesItemOnWrite listener recalculation"
    - from: "legacy seriesItems missing publishedToSubsplash"
      to: "strict-count model"
      via: "backfill script using real Subsplash series membership"
---

<objective>
Allow series publishing to work independently of list publishing, while preserving a one-click publish-to-everything option.

Purpose: align UI behavior with publishing intent and keep series counts semantically correct.
Output: updated publish UX + strict count derivation + legacy reconciliation + tests/docs.
</objective>

<execution_context>
@/Users/yasaad/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/01-series-subtitle-automation/01-CONTEXT.md
</execution_context>

<context>
@components/ManagePublishingPopup.tsx
@components/UploadStatusList.tsx
@pages/admin/series/[seriesId].tsx
@functions/src/addToSeries.ts
@functions/src/helpers/seriesHelpers.ts
@functions/src/DocumentListeners/Series/seriesItemOnWrite.ts
@functions/src/test/series/seriesMetadata.test.ts
@functions/src/test/series/addToSeries.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple series publish from list publish gating</name>
  <files>components/ManagePublishingPopup.tsx, functions/src/addToSeries.ts</files>
  <action>
    Update the publishing popup so series publish is independent from list publish status.

    Implementation specifics:
    - Keep hard requirement: sermon must already have `sermon.subsplashId` (media item exists in Subsplash).
    - Remove any UI/text gating that implies list publication is required before series publication.
    - Ensure `Publish to Series` remains available when sermon has a series and is not yet in that series, regardless of list status.
    - Keep existing behavior for creating Subsplash series on demand if local series has no `subsplashId`.

    Safety:
    - Do not auto-publish to lists as part of plain `Publish to Series`.
    - Preserve existing error handling and permission checks in callables.
  </action>
  <verify>
    - `rg -n "Upload to lists first|Publish to Series" components/ManagePublishingPopup.tsx`
    - Manual check: sermon with no published lists but valid `subsplashId` can still publish to series.
  </verify>
  <done>
    Series publish action no longer depends on list publishing state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add optional one-click "Publish Everywhere" convenience flow</name>
  <files>components/ManagePublishingPopup.tsx, components/UploadStatusList.tsx</files>
  <action>
    Add an explicit convenience action that publishes to lists and series together, while preserving independent actions.

    Implementation specifics:
    - Introduce a clearly labeled action (e.g., `Publish Everywhere`) in ManagePublishingPopup when sermon has `seriesId`.
    - Combined action should:
      1) publish to selected/all lists (existing list flow)
      2) publish to series (existing series flow)
      3) report partial failures clearly if one succeeds and the other fails.
    - Keep existing standalone actions:
      - `Publish to Subsplash` for lists-only
      - `Publish to Series` for series-only

    Safety:
    - Avoid duplicated network calls when sermon already published to some/all lists or already published to series.
    - Keep button disabled/loading semantics clear to prevent double-submit.
  </action>
  <verify>
    - Manual check: one click can publish both lists and series.
    - Manual check: series-only and lists-only paths still work independently.
  </verify>
  <done>
    Users can choose independent publishing or one-click combined publishing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce strict series-published counting model</name>
  <files>functions/src/helpers/seriesHelpers.ts, functions/src/DocumentListeners/Series/seriesItemOnWrite.ts, pages/admin/series/[seriesId].tsx</files>
  <action>
    Make series published count strict: only explicit `publishedToSubsplash === true` counts as published.

    Implementation specifics:
    - Remove fallback inference from `sermonSubsplashId` in helper derivation and UI data shaping.
    - Ensure listener and UI both consume the same strict interpretation.
    - Preserve that unpublish/deletion flows set `publishedToSubsplash: false` and clear stale linkage.

    Safety:
    - Do not conflate "has subsplash media item" with "is in this subsplash series".
    - Keep subtitle derivation tied to strict published count only.
  </action>
  <verify>
    - `rg -n "sermonSubsplashId.*published|fallback|hasExplicitPublishedState" functions/src/helpers/seriesHelpers.ts pages/admin/series/[seriesId].tsx`
    - Regression check: item with `sermonSubsplashId` but `publishedToSubsplash: false` is not counted.
  </verify>
  <done>
    Series count reflects actual series membership publication only.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add one-time legacy reconciliation/backfill path</name>
  <files>scripts/backfillSeriesPublishedFlags.ts, functions/src/helpers/seriesHelpers.ts, docs/series-publish-workflow.md</files>
  <action>
    Provide an explicit migration path for legacy series items missing/incorrect `publishedToSubsplash` flags.

    Implementation specifics:
    - Add a script that:
      - iterates Firestore series and seriesItems
      - fetches real series membership from Subsplash API (`getSeriesItems`)
      - updates each `seriesItem.publishedToSubsplash` based on actual membership match
      - writes a dry-run report and supports apply mode.
    - Document when and how to run it safely.

    Safety:
    - Default to dry-run.
    - Require explicit `--apply` to write changes.
    - Include idempotent updates and logging summary.
  </action>
  <verify>
    - Dry-run output includes counts of would-change items.
    - Apply mode updates only mismatched records.
  </verify>
  <done>
    Legacy data can be reconciled to strict semantics without app-level fallback hacks.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add tests for independent and combined publish flows</name>
  <files>functions/src/test/series/seriesMetadata.test.ts, functions/src/test/series/addToSeries.test.ts, functions/src/test/series/*.test.ts</files>
  <action>
    Expand tests to enforce the new behavior contract.

    Minimum coverage additions:
    - Series publish flow does not require list-published state.
    - Strict count logic uses only `publishedToSubsplash`.
    - Combined publish path exercises both list and series operations.
    - Unpublish from series/list updates state correctly without cross-coupling.

    If UI integration tests are unavailable, add focused unit tests for shared helpers and callable pathways plus manual QA checklist in docs.
  </action>
  <verify>
    - `cd functions && pnpm test -- series/seriesMetadata.test.ts`
    - `cd functions && pnpm test -- series/addToSeries.test.ts`
    - `cd functions && pnpm test -- series`
  </verify>
  <done>
    Regression coverage protects independent publish workflows and strict counting rules.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Sermon with no list publications can still be published to series from ManagePublishingPopup.
- [ ] "Publish Everywhere" action can publish to both lists and series in one flow.
- [ ] Series subtitle/count updates reflect only `publishedToSubsplash === true` items.
- [ ] Backfill dry-run and apply behaviors are documented and validated.
- [ ] `cd functions && pnpm test -- series` passes.
</verification>

<success_criteria>
- Series publishing is operationally independent from list publishing.
- Combined one-click publish path exists for convenience.
- Series published counts are semantically strict and deterministic.
- Legacy data reconciliation path exists without reintroducing runtime inference.
</success_criteria>

<output>
After completion, create `.planning/phases/01-series-subtitle-automation/01-02-SUMMARY.md`.
</output>
